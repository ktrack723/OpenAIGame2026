# PLANETPOOL — 기획서 v2.0

> **한 줄 정의**: 핵미사일을 중력 스윙바이로 휘어 쳐서 행성을 잡는 우주 당구 로그라이크.
> **레퍼런스**: Balatro(런/핸드/조커) × Peglin(발사 물리 + 탄종 오브) × KSP(진짜 중력 어시스트).
> **플랫폼 가정**: PC, 2D 톱다운, 60fps 렌더 / 120Hz 고정 스텝 시뮬. 1런 40~60분.
> **v1.0 대비**: 내용 삭제 없음. 수식을 **3계층으로 분류**하고, 설계 의도·온보딩·정보설계·사운드·리스크를 추가.

---

## 0. 이 문서를 읽는 법 — "수식 다 필요한가?"에 대한 답

**결론부터: 실제로 게임에 들어가는 런타임 수식은 9개뿐이다.** v1.0이 읽기 힘들었던 건 수식이 많아서가 아니라, **성격이 완전히 다른 세 종류의 수식이 같은 페이지에 섞여 있어서**다. 아래처럼 태그를 붙이고 나면 각각이 왜 있는지가 명확해진다.

| 계층 | 정의 | 실행 시점 | 개수 | 빼면? |
|---|---|---|---|---|
| **T1 · 런타임** | 매 프레임/매 샷 도는 코드 | 게임 중 | 9 | 게임이 안 돌아감 |
| **T2 · 생성타임** | 스테이지 로딩 때 1회 | 판 만들 때 | 6 | 게임은 돌지만 판이 구려짐 |
| **T3 · 설계근거** | 코드에 안 들어감. 종이 위 계산 | 튜닝할 때 | 5 | 아무 일도 안 일어남. 대신 밸런싱이 감(感)이 됨 |

### 0.1 T1 — 이것만 구현하면 게임은 돈다 (9개)

| # | 수식 | 하는 일 | 대체 가능? |
|---|---|---|---|
| T1-1 | 뉴턴 중력 + 소프트닝 | 궤도가 살아 있게 함 | ✕ 게임의 정체성 |
| T1-2 | KDK 리프프로그 | 궤도가 몇 분 뒤에도 안 터지게 함 | △ RK4도 되지만 에너지 드리프트로 궤도가 새어나감 |
| T1-3 | 당구감 계수 κ | 미사일이 실제로 휘게 함 | ✕ **이거 없으면 게임이 성립 안 함** (§4.3) |
| T1-4 | 스윙바이 판정 (6R / δ≥25°) | "잘 쳤다"를 판정 | ✕ 점수 시스템의 근간 |
| T1-5 | 데미지 공식 | 표면 파괴 | △ 상수로 뭉개도 되지만 속도 연동을 잃음 |
| T1-6 | 임펄스 Δv = J/μ_p | 행성을 밈 | ✕ 당구의 "밀림" |
| T1-7 | 파쇄 문턱 v_shatter | 합성 vs 개박살 분기 | ✕ 목표 ③의 근간 |
| T1-8 | 점수 배율 M | 보상 | ✕ |
| T1-9 | R_p, HP ← μ 파생 | 질량 하나로 외형·내구 결정 | ○ 룩업 테이블로 대체 가능. 근데 수식이 더 쌈 |

**즉 "구현만 하면 되는" 범위에서 진짜 외워야 할 건 9줄이다.**

### 0.2 T2 — 판을 잘 뽑기 위한 것 (6개, 로딩 중 1회)

`힐 간격 배치` · `궤도요소→상태벡터` · `로그균등 질량` · `차폐도 B` · `안정성 사전시뮬` · `솔버 해율`.

이 계층은 **런타임 성능과 무관**하다. 로딩 중에 한 번 돌고 끝. 그래서 여기 있는 수식은 "복잡해서 부담"이 아니라 "판 품질에 쓰는 예산"으로 봐야 한다. 다만 v1.0의 솔버 사양에는 실제 성능 문제가 있어서 §7.7에서 고쳤다.

### 0.3 T3 — 코드에 안 들어가는 것 (5개)

`v_c=√(μ★/a)` · `Δa≈2aΔv/v` · `Gladman 3.46` · `굴절각 δ=2·arcsin(1/e)` · `SOI 확대율 κ^(2/5)`.

**이건 구현 스펙이 아니라 튜닝 도구다.** "행성 질량을 2배로 올리면 스윙바이가 얼마나 더 휘나?"를 시뮬 안 돌리고 5초 만에 답하기 위한 계산기. 프로그래머는 무시해도 되고, 밸런서는 이걸 잃으면 눈 감고 숫자를 만지게 된다.

### 0.4 실제로 빼도 되는 것 (수식 다이어트)

정직하게, v1.0에서 **뺄 수 있는 건 아래 7개**다. 나머지는 각자 일을 한다.

| 항목 | 판정 | 이유 |
|---|---|---|
| Rayleigh 이심률 샘플링 | **→ `U(0, e_max)`로 강등** | 천문학적으로는 옳지만 화면에서 구분 불가. 유지비 0이라 취향 문제 |
| 8단계 해율 밴드 | **→ 3단으로 축소** | 텔레메트리 없이 8단 밴드를 지킬 방법이 없음. 지킬 수 없는 정밀도 |
| 4032발 솔버 그리드 | **→ 2단계 적응형** | v1.0 사양은 최악 10초 히치 (§7.7에 계산과 수정안) |
| 오버플로 해소 8회 루프 | **→ 2회 + N−1** | 8회까지 갈 확률이 사실상 0. 죽은 코드 |
| 인접 질량비 >8 재샘플 | **→ 삭제** | 로그균등 샘플링이면 자연히 섞임. 중복 안전장치 |
| 역행 행성 (12%) | **→ P2로 이동** | 재미있지만 P0에 없어도 됨 |
| 카오스 계 모디파이어 | **→ P2로 이동** | 위와 동일 |

**반대로, v1.0에서 과소평가된 게 하나 있다: 차폐도 `B`.** 이건 "중간 행성은 치면 안 된다 / 볼링처럼 굴리면 안 된다"는 **목표 경험을 코드로 강제하는 유일한 장치**다. v1.0에서는 STEP 5의 한 줄이었지만, v2.0에서는 §7.5로 승격했다.

---

## 1. 이 게임이 무엇인가 — 내러티브 앵커

### 1.1 목표 경험

> **"핵미사일을 행성 중력에 휘게 해서, 저 뒤에 있는 놈을 친다."**

여기서 지켜야 할 감각이 셋이다.

1. **직사(直射)는 실패다.** 가운데 행성들은 건드리면 안 되는 장애물이다. 조준선 그어서 쏘면 되는 판이 나오면 안 된다. → §7.5 차폐도가 이걸 보장한다.
2. **볼링이 아니다.** 그냥 굴려서 부딪히는 게 아니라, **궤적이 휘어야** 한다. → §4.3 κ가 이걸 보장한다.
3. **판은 살아 있다.** 행성은 고정 장애물이 아니라 서로 끌어당기며 궤도가 변한다. 내가 하나를 밀면 그 뒤의 모든 정렬이 바뀐다. → §4.1 N체가 이걸 보장한다.

이 셋이 각각 코드 어디에 대응되는지를 명시하는 게 이 문서의 목적이다.

### 1.2 왜 핵을 쏘는가 — 3대 내러티브 축 ↔ 메카닉 매핑

| # | 내러티브 이유 | 게임 목표 유형 | 요구되는 플레이 | 대응 수식 |
|---|---|---|---|---|
| ① | **전쟁 중인 조르그 행성 말살** | 섬멸 | 스윙바이로 우회 타격 (직사 불가) | 차폐도 B, HP/데미지 |
| ② | **태양이 뜨거워짐 → 지구 궤도 후퇴** | 궤도 시프트 | 다른 행성을 큐볼로 써서 지구를 밈 | 임펄스 J/μ, Δa |
| ③ | **행성 충돌로 신규 행성 합성** | 합성 | 두 행성을 *살살* 부딪히게 유도 | v_shatter, 합성표 |

세 축이 전부 인트로 컷씬 하나(§12.1)로 정당화된다. ①②는 오프닝에서, ③은 액트 1 중 박사의 통신 한 줄로.

### 1.3 왜 이 조합이 새로운가

- **Peglin**은 쏘고 나면 끝이다. 여기서는 쏘고 나서도 **판이 계속 변한다**(N체).
- **Balatro**는 셔플이 자원 소모다. 여기서는 **기다리는 것이 셔플**이다(§3.2).
- **KSP**는 스윙바이가 목적이다. 여기서는 스윙바이가 **점수 배율**이다.

---

## 2. 코어 루프 & 런 구조

```
[런 시작] → 안테 1 ~ 8
  각 안테 = 스몰 작전 → 빅 작전 → 보스 작전 (3스테이지)
  스테이지: PCG 성계 생성 → 로켓 4발(보스 5발)로 목표 달성
    샷 루프: 관측(시간 스크럽) → 조준(각도·파워) → 발사
             → 스윙바이 체인 → 명중/파울 판정 → 점수
  클리어 → 연구비 정산 → 상점(탄종/유물/소모품) → 다음 스테이지
[실패 조건] 로켓 소진, 지구 파괴, 외교 게이지 3
```

- **로켓 = 핸드 카운트.** 스테이지당 기본 4발, 다 쓰면 실패.
- **관측 스킵 = 셔플.** 시간을 4배속으로 흘려보내면 행성 위상이 바뀐다. 별도 재화 소모 없음(대신 "신속 작전" 보너스를 포기). *기다리는 것 자체가 리롤*이라는 게 이 게임의 셔플 철학.
- 행성 궤도는 N체 상호작용으로 스테이지 내내 **계속 변한다.** 플레이어가 행성을 밀면 그 뒤의 모든 정렬이 바뀐다.

### 2.1 한 샷의 심리 사이클 (이게 재미의 최소 단위다)

```
① 본다      — "저 목표는 지금 가스행성 뒤에 숨어 있다"
② 기다린다   — "6초 뒤면 가스행성이 비켜난다. 근데 그때 얼음행성이 들어오네"
③ 발견한다   — "아, 그 얼음행성으로 감아 돌리면 되겠는데?"      ← 여기가 절정
④ 조준한다   — 각도·파워·발사시각 3축
⑤ 본다       — 미사일이 정말 휜다. 체인 카운터가 오른다
⑥ 결과       — 명중이면 대량 점수 / 빗나가면 "왜" 가 보여야 함  ← §14.4
```

**③이 이 게임의 상품이다.** 나머지 모든 시스템은 ③이 자주, 그러나 공짜가 아니게 일어나도록 하는 장치다. 밸런싱이 막히면 항상 "이 변경이 ③을 늘리는가 줄이는가"로 되묻는다.

---

## 3. 설계 기둥 5개

구현 중 판단이 갈릴 때 이 다섯 개로 결정한다.

### P1. 직사 금지
목표를 향해 그냥 쏴서 맞으면 그 판은 실패한 판이다. PCG가 **차폐도 B ≥ 0.6**을 강제해 직선 회랑을 막는다(§7.5). 이게 "볼링이 아니라 당구"의 코드적 정의.

### P2. 기다리는 것이 리롤
재화를 쓰는 셔플이 없다. 대신 시간을 흘리면 판이 바뀐다. 조급함에는 "신속 작전 +1$" 보상이, 신중함에는 좋은 정렬이 붙는다. **인내가 자원이다.**

### P3. 잘 친 샷이 곧 화력
스윙바이는 점수 배율만 주는 게 아니라 **실제로 미사일을 가속시켜 데미지를 올린다**(데미지 공식의 `v_imp` 항). 예쁘게 친 샷과 센 샷이 같은 것 — 이게 이 게임에서 스타일과 효율이 갈라지지 않는 이유.

### P4. 궤도가 변한 건 내 탓이다
플레이어 임펄스가 만든 궤도 변화는 **반드시 시각적으로 귀속**되어야 한다. 자연 섭동은 은은하게(태양 인력의 0.1~1%), 플레이어 타격은 극적으로(Δa ≈ 100 GU). 트레일 색 강조로 "저건 내가 민 거다"를 못 박는다(§14.2).

### P5. 실패는 읽혀야 한다  ⟵ v2.0 신규
N체 물리 게임의 최대 사망 원인은 **"왜 빗나갔는지 모르겠음"**이다. 빗나간 샷마다 자동으로 원인 태그를 1개 띄운다: `파워 부족` / `발사 너무 빠름` / `굴절 부족(24°, 25° 필요)` / `포획됨`. 이거 없으면 플레이어는 학습을 못 하고 게임은 운빨겜으로 인식된다. → §14.4

---

## 4. 물리 시스템 사양  ⟨T1⟩

### 4.1 단위계

- 거리 GU, 시간 s. **G = 1**로 정규화 → 질량 대신 중력 파라미터 μ(= G·m)를 저장하며, 수치상 μ = m.
- 태양: `μ★ = 3.0×10⁵`, 반지름 `R★ = 40`. **원점 고정**(움직이지 않음).
- 행성: `μ_p ∈ [80, ~2500]` (§7에서 안테별 샘플링), 반지름 `R_p = 1.6·μ_p^(1/3)` (μ80→6.9, μ300→10.7, μ730→14.4).
- 지구: `μ_E = 300` 고정, `R_E = 11`.

> **왜 태양을 고정하는가**: 엄밀히는 태양도 행성 인력을 받아야 하지만(운동량 보존), 화면 중심이 미세하게 흔들리면 궤도 트레일의 가독성이 무너진다. 태양 질량이 행성의 400~3750배라 물리적 오차도 무시 가능. **의도적 근사이며, 이 결정은 P4(궤도 변화 귀속)를 위한 것**이다.

**스케일 근거** ⟨T3⟩: 최내곽 궤도 a=260에서 공전속도 `v_c = √(μ★/a) ≈ 34 GU/s`, 주기 `T = 2πa/v_c ≈ 48 s`. 즉 안쪽 행성이 스테이지(2~4분) 동안 2~5바퀴 돌아 "움직이는 표적" 느낌을 주되, 조준 불가능할 만큼 빠르진 않다. 외곽(a≈1400)은 T≈10분 — 거의 고정 장애물 역할. **이 한 줄이 `a_min=260`이라는 숫자의 전부다.** 더 작으면 정신없고, 더 크면 판이 정지화면이 된다.

### 4.2 중력 & 적분

모든 천체 쌍에 뉴턴 중력(플러머 소프트닝 ε=3):

```
a_i = Σ_{j≠i}  μ_j · (r_j − r_i) / (|r_j − r_i|² + ε²)^(3/2)
```

- **적분기**: 킥-드리프트-킥(KDK) 리프프로그, 고정 `dt = 1/120 s`. 심플렉틱이라 에너지 드리프트가 없어 궤도가 장시간 안정.
- **미사일**: 질량 없는 시험입자(행성을 끌지 않음 — 예외: 특수탄 §10). 행성과의 거리 `d < 5R_p`이면 dt/4 서브스텝.
- **결정론**: 고정 dt + 고정 순회 순서 + 시드 PRNG(xoshiro256**) → 시드 공유·리플레이·데일리 런 가능.

> **ε=3이 하는 일**: 두 천체가 거의 겹칠 때 `1/r²`가 발산해서 적분기가 폭발하는 걸 막는다. 3 GU는 최소 행성 반지름(6.9)의 절반 이하라 정상 궤도에는 영향이 없다. **버그 방지용 상수이지 게임 디자인 상수가 아니다.**

### 4.3 당구감 계수 κ = 60 — 이 게임에서 가장 중요한 상수

실측 스케일로는 빠른 미사일이 행성 곁을 지나도 **거의 안 휜다.** 그래서 미사일이 받는 **행성** 중력에만 배율 κ를 곱한다. 태양 중력에는 안 곱한다.

```
a_missile = grav(태양, κ=1) + Σ grav(행성_j, κ=60)
```

- **행성끼리는 진짜 물리**(κ=1)라서 궤도 표류는 정직하게 유지된다. 즉 κ는 *플레이어 경험*만 왜곡하고 *세계*는 왜곡하지 않는다.
- 미사일이 시험입자라 **작용-반작용을 깨도 운동량 오차가 발생하지 않는다.** κ가 물리적으로 "공짜"인 이유.
- 실효 영향권(SOI)은 `κ^(2/5) ≈ 5.1배`로 넓어진다 ⟨T3⟩ → 행성 하나의 "당구 쿠션 범위"가 5배가 된다는 뜻. 스윙바이 판정 반경을 6R_p로 잡은 근거.

**설계 결정**: κ는 게임 밸런스 노브다. 유물 "마흐의 원리"(κ +25%)가 파워 유물인 이유가 이것.

#### 굴절각 계산기 ⟨T3⟩ — 시뮬 안 돌리고 답 내는 법

쌍곡선 통과의 굴절각 δ는 닫힌 해가 있다:

```
e = 1 + r_p · v_∞² / (κ · μ_p)          # r_p = 최근접거리, v_∞ = 통과 속력
δ = 2 · arcsin(1 / e)
```

이걸 쓰면 밸런싱 질문에 즉답할 수 있다:

| 행성 | 통과거리 | 미사일 속력 | 굴절각 δ | 판정 |
|---|---|---|---|---|
| μ300 (R10.7) | 3R_p = 32 | 40 | **30.2°** | ✅ 스윙바이 성립 |
| μ300 | 3R_p = 32 | 60 | 15.6° | ❌ 25° 미달 |
| μ730 (R14.4) | 2R_p = 29 | 60 | **34.5°** | ✅ |
| μ80 (R6.9) | 3R_p = 21 | 40 | 14.6° | ❌ 잔챙이론 못 휨 |

**여기서 게임의 핵심 트레이드오프가 튀어나온다 (P3의 물리적 근거):**

> **느리게 쏘면 잘 휘지만 데미지가 약하고, 빠르게 쏘면 세지만 안 휜다.**

데미지 공식이 `v_imp`에 비례하므로(§6.1), 정답은 **"느리게 쏴서 스윙바이로 가속시킨 뒤 때린다"**가 된다. 이게 이 게임이 가르치려는 단 하나의 기술이다. **의도된 설계가 아니라 물리에서 자연히 나온 것 — 그래서 좋다.**

### 4.4 구현 — 물리 코어 (TypeScript)

```ts
// ─── 상수 (부록 A와 1:1 대응) ────────────────────────────
export const CFG = {
  DT:       1 / 120,   // 고정 스텝
  EPS:      3,         // 소프트닝 (버그 방지)
  KAPPA:    60,        // 당구감 계수 (게임 디자인)
  MU_STAR:  3.0e5,
  R_STAR:   40,
  SUBSTEP_DIST: 5,     // d < 5·R_p 이면 서브스텝
  SUBSTEP_N:    4,
} as const;

export interface Vec2 { x: number; y: number; }
const v2 = (x = 0, y = 0): Vec2 => ({ x, y });

export interface Body {
  mu: number; radius: number;
  pos: Vec2; vel: Vec2;
  hp: number; hpMax: number;
  type: PlanetType;
  isEarth: boolean;
  alive: boolean;
}

export const radiusOf = (mu: number) => 1.6 * Math.cbrt(mu);   // T1-9
export const hpOf     = (mu: number) => 12 + mu / 12;          // T1-9

// ─── T1-1: N체 가속도 (태양은 고정, 힘은 준다) ─────────────
function accelOn(i: number, bodies: Body[], out: Vec2): void {
  const bi = bodies[i];
  // 태양 (원점 고정)
  let dx = -bi.pos.x, dy = -bi.pos.y;
  let d2 = dx * dx + dy * dy + CFG.EPS * CFG.EPS;
  let inv = CFG.MU_STAR / (d2 * Math.sqrt(d2));
  out.x = dx * inv; out.y = dy * inv;

  // 행성 상호작용
  for (let j = 0; j < bodies.length; j++) {
    if (j === i || !bodies[j].alive) continue;
    const bj = bodies[j];
    dx = bj.pos.x - bi.pos.x;
    dy = bj.pos.y - bi.pos.y;
    d2 = dx * dx + dy * dy + CFG.EPS * CFG.EPS;
    inv = bj.mu / (d2 * Math.sqrt(d2));
    out.x += dx * inv; out.y += dy * inv;
  }
}

// ─── T1-2: KDK 리프프로그 (심플렉틱) ──────────────────────
const _acc: Vec2[] = [];
export function stepBodies(bodies: Body[], dt: number): void {
  while (_acc.length < bodies.length) _acc.push(v2());
  const h = dt * 0.5;

  for (let i = 0; i < bodies.length; i++)                  // KICK
    if (bodies[i].alive) { accelOn(i, bodies, _acc[i]);
      bodies[i].vel.x += _acc[i].x * h; bodies[i].vel.y += _acc[i].y * h; }

  for (const b of bodies)                                   // DRIFT
    if (b.alive) { b.pos.x += b.vel.x * dt; b.pos.y += b.vel.y * dt; }

  for (let i = 0; i < bodies.length; i++)                  // KICK
    if (bodies[i].alive) { accelOn(i, bodies, _acc[i]);
      bodies[i].vel.x += _acc[i].x * h; bodies[i].vel.y += _acc[i].y * h; }
}

// ─── T1-3: 미사일 (시험입자 + κ 증폭) ─────────────────────
export interface Missile {
  pos: Vec2; vel: Vec2;
  power: number;          // 탄종 위력 P
  kappaMul: number;       // 유물 "마흐의 원리" 등
  alive: boolean;
  chain: number;          // 스윙바이 체인
  nearMiss: number;
  perihelionBonus: boolean;
}

function accelMissile(m: Missile, bodies: Body[], out: Vec2): void {
  let dx = -m.pos.x, dy = -m.pos.y;
  let d2 = dx * dx + dy * dy + CFG.EPS * CFG.EPS;
  let inv = CFG.MU_STAR / (d2 * Math.sqrt(d2));     // ← 태양은 κ 미적용
  out.x = dx * inv; out.y = dy * inv;

  const k = CFG.KAPPA * m.kappaMul;
  for (const b of bodies) {
    if (!b.alive) continue;
    dx = b.pos.x - m.pos.x; dy = b.pos.y - m.pos.y;
    d2 = dx * dx + dy * dy + CFG.EPS * CFG.EPS;
    inv = (k * b.mu) / (d2 * Math.sqrt(d2));        // ← 행성만 κ 증폭
    out.x += dx * inv; out.y += dy * inv;
  }
}

export function stepMissile(m: Missile, bodies: Body[], dt: number): void {
  // 근접 시 서브스텝: 스윙바이 구간의 적분 오차가 곧 "빗나감"이므로 필수
  let n = 1;
  for (const b of bodies) {
    if (!b.alive) continue;
    const dx = b.pos.x - m.pos.x, dy = b.pos.y - m.pos.y;
    if (dx * dx + dy * dy < (CFG.SUBSTEP_DIST * b.radius) ** 2) { n = CFG.SUBSTEP_N; break; }
  }
  const sd = dt / n, a = v2();
  for (let s = 0; s < n; s++) {
    accelMissile(m, bodies, a);
    m.vel.x += a.x * sd * 0.5; m.vel.y += a.y * sd * 0.5;
    m.pos.x += m.vel.x * sd;   m.pos.y += m.vel.y * sd;
    accelMissile(m, bodies, a);
    m.vel.x += a.x * sd * 0.5; m.vel.y += a.y * sd * 0.5;
  }
}

// ─── T3: 굴절각 계산기 (밸런싱 툴. 런타임 아님) ────────────
export function deflectionDeg(muPlanet: number, periapsis: number, vInf: number): number {
  const e = 1 + (periapsis * vInf * vInf) / (CFG.KAPPA * muPlanet);
  return (2 * Math.asin(1 / e) * 180) / Math.PI;
}
```

---

## 5. 발사 · 스윙바이 · 판정 규칙  ⟨T1⟩

### 5.1 발사 UI

- 파라미터 3개: **각도**(연속), **파워** `P_v ∈ [40, 140] GU/s`, **발사 시각**(시간 스크럽).
- **관제실 모드**(기본 ON): 조준 중 시간 정지 + 미사일 예측선 3초(행성 궤도 프리즈 근사). 하드 모드는 OFF — 실시간 조준.
- 예측선은 기본적으로 "지금 이 순간의 중력장" 근사. 유물 "라플라스의 악마"를 얻으면 행성 이동까지 반영한 풀 시뮬 예측으로 업그레이드(§10).

> **왜 예측선을 일부러 부정확하게 두는가**: 완전 정확한 예측선을 처음부터 주면 게임이 "선 보고 맞추기"가 된다. 부정확한 3초 예측선은 **플레이어에게 물리 직관을 강제로 학습시키는 장치**이고, "라플라스의 악마"는 그 학습을 돈 주고 건너뛰는 보상이다. 정보를 파워업으로 파는 구조.

### 5.2 스윙바이 판정

```
행성별 인카운터 추적:
  진입: d < 6·R_p 시 그 순간의 속도 v_in 기록 (태양 기준계)
  이탈: d > 6·R_p 시 v_out과 비교, 굴절각 δ = angle(v_in, v_out)
  δ ≥ 25° AND 무접촉  → 스윙바이 성립, 체인 +1
보조 판정:
  니어미스: 인카운터 중 d_min < 1.8·R_p (무접촉) → 배율 +0.25
  포획: 행성 주위 ¾바퀴 이상 감김 → 강제 추락 = 그 행성 직격으로 처리
```

저파워로 목성형에 접근하면 진짜로 포획당해 처박힌다(κ·μ 기준 국소 탈출속도보다 느리면). **리스크로 명시** — 이게 "무조건 느리게 쏘면 됨"을 막는 안전장치다.

### 5.3 파울과 외교

- 스테이지마다 **LEGAL_HIT 집합**이 정의된다. 섬멸전 = {목표}, 합성전 = {지정 큐·오브젝트 행성}, 궤도 시프트 = {보호 마크 제외 전부}.
- LEGAL_HIT 밖 행성(또는 지구, 보호 행성) 직격 = **파울**: 해당 샷 점수 0 + 외교 게이지 +1. **게이지 3 = 즉시 스테이지 실패** ("은하 여론 악화").
- 파편·소행성 충돌은 파울 아님(미사일만 소멸).
- 지구 HP 3. 자기 미사일·파편·행성 스침으로 −1. 0이면 런 종료.

> **외교 게이지가 하는 진짜 일**: "중간 행성은 치면 안 된다"는 목표 경험을 **처벌**로 못 박는 장치. 이게 없으면 플레이어는 그냥 다 부수고 다닌다. 3회 유예를 준 건 학습 곡선을 위한 것 — 1회 실패는 가혹하고, 무제한은 규칙이 없는 것과 같다.

### 5.4 구현 — 인카운터 트래커 (TypeScript)

```ts
const SWINGBY_ZONE   = 6.0;    // ×R_p
const SWINGBY_DEG    = 25;
const NEARMISS_ZONE  = 1.8;
const CAPTURE_WRAP   = 0.75;   // 바퀴

interface Encounter { vIn: Vec2; dMin: number; wrap: number; lastAngle: number; }

export class EncounterTracker {
  private open = new Map<Body, Encounter>();

  update(m: Missile, bodies: Body[], onFoul: (b: Body) => void): void {
    for (const b of bodies) {
      if (!b.alive) continue;
      const dx = m.pos.x - b.pos.x, dy = m.pos.y - b.pos.y;
      const d = Math.hypot(dx, dy);
      const zone = SWINGBY_ZONE * b.radius;
      const enc  = this.open.get(b);

      if (d < zone) {
        const ang = Math.atan2(dy, dx);
        if (!enc) {
          this.open.set(b, { vIn: { ...m.vel }, dMin: d, wrap: 0, lastAngle: ang });
        } else {
          enc.dMin = Math.min(enc.dMin, d);
          let dA = ang - enc.lastAngle;                 // 감긴 각 누적
          while (dA >  Math.PI) dA -= 2 * Math.PI;
          while (dA < -Math.PI) dA += 2 * Math.PI;
          enc.wrap += Math.abs(dA) / (2 * Math.PI);
          enc.lastAngle = ang;
          if (enc.wrap >= CAPTURE_WRAP) {               // 포획 → 강제 추락
            this.open.delete(b); onFoul(b); m.alive = false; return;
          }
        }
      } else if (enc) {                                  // 이탈 → 굴절각 정산
        const dot = enc.vIn.x * m.vel.x + enc.vIn.y * m.vel.y;
        const den = Math.hypot(enc.vIn.x, enc.vIn.y) * Math.hypot(m.vel.x, m.vel.y);
        const deg = (Math.acos(Math.max(-1, Math.min(1, dot / den))) * 180) / Math.PI;
        if (deg >= SWINGBY_DEG) m.chain++;
        if (enc.dMin < NEARMISS_ZONE * b.radius) m.nearMiss++;
        this.open.delete(b);
      }
    }
  }
}
```

---

## 6. 데미지 · 궤도 변화 · 행성 충돌  ⟨T1⟩

### 6.1 표면 파괴

```
행성 내구 풀   HP  = 12 + μ_p/12                         (μ300→37, μ730→73)
샷 데미지     dmg = P · (0.5 + 0.5·min(v_imp/120, 2)) · (1 + 0.2·스윙바이체인)
              P = 탄 위력(표준 10), v_imp = 충돌 속력
```

세 항이 각각 다른 걸 보상한다:

| 항 | 범위 | 보상하는 것 |
|---|---|---|
| `P` | 1~40 | 상점에서 산 탄종 (빌드) |
| `(0.5 + 0.5·min(v/120,2))` | 0.5 ~ 1.5 (명목) | 충돌 속력 = **스윙바이 가속의 결실** |
| `(1 + 0.2·chain)` | 1.0 ~ 1.8 | 체인 자체 (스타일) |

> **P3의 구현체.** 표준탄 직사(v=60)면 `10 × 0.75 × 1.0 = 7.5`. 2체인 감아서 v=140으로 때리면 `10 × 1.08 × 1.4 = 15.2`. **똑같은 탄인데 잘 치면 두 배.** 이 수식을 상수로 뭉개면 게임의 절반이 죽는다.
>
> ⚠️ **밸런스 지적(v2.0)**: 속도항이 최대 1.5가 되려면 `v_imp ≥ 240`인데, 실제 스윙바이로 얻는 속력은 대략 140~180이다. 즉 **명목 상한 1.5는 도달 불가능하고 실효 범위는 0.5~1.17**이다. 스윙바이의 보상이 설계 의도보다 20% 작다.
> → 수직 슬라이스에서 실측 v_imp 분포를 보고 `min(v/120,2)` → **`min(v/90, 2)`** 로 정규화 상수를 내리는 것을 권장(그러면 v=180에서 1.5 도달). *v1.0 값을 지우지 않고 남겨두되, 부록 A에 튜닝 후보로 표기.*

- 텍스처 데미지 4단계: `state = ⌊(1 − HP/HP_max)·4⌋`. 충돌 지점의 행성-로컬 좌표에 크레이터 데칼 스플랫 + 단계별 균열 오버레이. 스윙바이로 가속해서 때리면 더 아프다 — "잘 친 샷"이 화력이기도 하다.
- HP 0 → 파괴: 파편 `n = clamp(4+⌊μ/150⌋, 4, 9)`개 생성, 총질량은 원본의 55%(나머지 증발), 각 파편은 폭심 반대 반구로 산개 초기속도를 받고 이후 N체에 합류. 파편은 소행성 해저드(맞으면 미사일 소멸, 파울 아님).

### 6.2 임펄스 (행성 밀기)

```
J = 75 · P
방향 d̂ = normalize( 0.7·v̂_missile + 0.3·n̂_impact )   # 관통감 70 + 크레이터 반발 30
Δv_planet = J / μ_p  ·  d̂
```

- 표준탄(P=10)이 μ300 행성에 **Δv ≈ 2.5 GU/s** — 접선 방향이면 `Δa = 2a·Δv/v_c` ⟨T3⟩ 만큼 궤도가 이동한다. 위치 의존적이다: **a=400에서 73 GU, a=630에서 144 GU, a=900에서 246 GU.** (안쪽일수록 잘 안 밀린다 — 태양에 꽉 잡혀 있으니까.) 대표값 ≈100.
- 무거운 놈(μ730)은 Δv 1.0밖에 안 밀린다 → 헤비급은 직접 못 밀고 **다른 행성을 당구공 삼아** 박아야 한다. 전략의 축.
- **정밀 제어는 각도로 한다**: 임펄스가 접선과 θ만큼 어긋나면 궤도 이동에 기여하는 건 `Δv·cos θ`뿐이다. 즉 플레이어는 **때리는 각도로 Δa를 0부터 최대까지 연속 조절**할 수 있다. 궤도 시프트 목표(§8)의 좁은 밴드를 맞추는 게 가능한 이유가 이것이며, **이 사실을 플레이어에게 가르쳐야 한다**(§13 A2 빅).
- `Δv = J/μ` 라는 **단 한 줄이 이 게임의 무게 개념 전부**다. 가벼운 놈은 큐볼, 무거운 놈은 벽. 별도 "질량 등급" 시스템이 필요 없다.

### 6.3 행성 ↔ 행성 충돌 (합성/파쇄)

```
파쇄 문턱  v_shatter = 2.5 · √( 2(μ_i+μ_j) / (R_i+R_j) )     # = 2.5 × 상호 탈출속도
상대속도 < v_shatter → 융합: 완전 비탄성
    μ' = μ_i+μ_j,  v' = (μ_i v_i + μ_j v_j)/μ',  위치 = 질량중심, 타입 = 합성표(§11)
상대속도 ≥ v_shatter → 파쇄: 양쪽 모두 §6.1의 파편 규칙으로 산산조각
```

자연 표류로 살살 부딪치면 합성, 핵으로 후려박은 고속 충돌은 개박살 — **목적에 따라 속도를 조절해야 한다.** 목표 ③(합성)과 목표 ①(섬멸)이 같은 충돌 시스템에서 갈라지는 지점.

> **왜 탈출속도 기준인가**: 실제 천체 형성에서 부착(accretion)과 파괴적 충돌을 가르는 물리적 문턱이 정확히 이것이다. 2.5는 게임 상수지만 √(2Σμ/ΣR) 부분은 공짜로 얻은 정당성 — 무거울수록 잘 뭉치고, 클수록 잘 부서지는 직관이 자동으로 나온다.

### 6.4 구현 — 충돌 해결 (TypeScript)

```ts
export function resolveMissileHit(m: Missile, b: Body, legalHits: Set<Body>, st: StageState) {
  const vImp = Math.hypot(m.vel.x, m.vel.y);

  if (!legalHits.has(b)) {                                   // §5.3 파울
    st.diplomacy++; st.shotScore = 0; m.alive = false;
    if (st.diplomacy >= 3) st.fail('DIPLOMATIC_INCIDENT');
    return;
  }

  // T1-5 데미지
  const dmg = m.power * (0.5 + 0.5 * Math.min(vImp / 120, 2)) * (1 + 0.2 * m.chain);
  b.hp -= dmg;
  b.textureState = Math.floor((1 - b.hp / b.hpMax) * 4);     // 0~3, 텍스처 스왑

  // T1-6 임펄스
  const J  = 75 * m.power;
  const nx = (b.pos.x - m.pos.x) / (Math.hypot(b.pos.x - m.pos.x, b.pos.y - m.pos.y) || 1);
  const ny = (b.pos.y - m.pos.y) / (Math.hypot(b.pos.x - m.pos.x, b.pos.y - m.pos.y) || 1);
  let dx = 0.7 * (m.vel.x / vImp) + 0.3 * -nx;
  let dy = 0.7 * (m.vel.y / vImp) + 0.3 * -ny;
  const dl = Math.hypot(dx, dy); dx /= dl; dy /= dl;
  const dv = J / b.mu;
  b.vel.x += dx * dv; b.vel.y += dy * dv;

  st.pushTrailHighlight(b);                                  // P4: "내가 민 것" 표시
  m.alive = false;
  if (b.hp <= 0) shatter(b, st);
}

// T1-7 행성끼리 충돌
export function resolveBodyPair(a: Body, b: Body, st: StageState) {
  const vRel = Math.hypot(a.vel.x - b.vel.x, a.vel.y - b.vel.y);
  const vShatter = 2.5 * Math.sqrt((2 * (a.mu + b.mu)) / (a.radius + b.radius));
  if (vRel < vShatter) fuse(a, b, st);   // 합성 → §11 합성표
  else { shatter(a, st); shatter(b, st); }
}
```

---

## 7. PCG 스테이지 생성  ⟨T2⟩

### 7.1 설계 원칙

1. **통제된 불안정**: 상호 힐 반경 간격으로 "당장은 안 무너지지만 서서히 뒤틀리는" 성계를 만든다. 2행성 안정 한계는 Gladman 기준 `Δ > 2√3 ≈ 3.46` 상호 힐 반경 ⟨T3⟩ — 이 **바로 위 구간(3.6~8.5)에서 논다.** 안정 한계를 아는 이유는 지키려고가 아니라 *간당간당하게 걸치려고*다.
2. **행성간 섭동은 은은하게, 플레이어 임펄스는 극적으로** (P4): μ_p를 μ★의 0.03~0.8% 수준으로 잡으면 자연 섭동 가속도는 태양 인력의 0.1~1% → 궤도가 분 단위로 서서히 밀린다. 궤도의 *큰* 변화는 플레이어의 핵 임펄스(Δa ≈ 100 GU/타, §6.2)가 만든다.
3. **생성 후 검증(rejection sampling)**: 만들고 → 안정성 시뮬 → 솔버로 풀이 가능성 확인 → 불합격이면 변이. **순수 생성만으로 해 존재를 보장하려 하지 않는다.** 수학으로 증명하는 대신 무식하게 쏴 보고 확인하는 게 훨씬 싸고 확실하다.

### 7.2 안테별 파라미터 테이블

| 파라미터 | 수식 (A = 안테 1~8) | A=1 | A=4 | A=8 |
|---|---|---|---|---|
| 행성 수 N | `3 + ⌊(A+1)/2⌋` (+1 확률 35%), cap 8 | 4~5 | 5~6 | 7~8 |
| μ 상한 | `μ_hi = 250 + 60A` | 310 | 490 | 730 |
| μ 하한 | 80 고정 | | | |
| 목성형 | 10% 확률로 1개 μ×3 (cap 2500) | | | |
| 힐 간격 K | `U(K_lo, K_hi)`, `K_hi = 9−0.5A`, `K_lo = max(3.6, K_hi−2)` | 6.5~8.5 | 5.0~7.0 | 3.6~5.0 |
| 이심률 σ | `e ~ Rayleigh(0.02+0.012A)`, clamp 0.25 | ~0.03 | ~0.07 | ~0.12 |
| 궤도 대역 | `a_min=260`, `a_max = 1150 + 90·min(A,6)` | 1240 | 1510 | 1690 |
| 역행 행성 | A≥5에서 12% 확률 1개 (간격 K×1.6 적용) ⟨P2로 이동⟩ | — | — | 가끔 |
| 로켓 수 | 스몰/빅 4, 보스 5 | | | |

> **이 표가 난이도의 전부다.** 행성이 늘고(N↑), 간격이 조여지고(K↓), 궤도가 찌그러진다(e↑). 셋 다 "정렬을 예측하기 어려워짐"이라는 한 방향을 가리킨다.
> **다이어트 노트**: `e ~ Rayleigh(σ)`는 `U(0, 2.2σ)`로 바꿔도 화면상 구분이 안 된다. Rayleigh는 천문학적 정확성이고, 유지비가 0이라 남겼을 뿐이다.

### 7.3 생성 알고리즘

**STEP 1 — 질량**
```
u_i ~ U(0,1)
μ_i = 80 · (μ_hi/80)^u_i        # 로그 균등: 잔챙이 다수 + 이따금 헤비급
```
> 로그 균등인 이유: 선형 균등이면 전부 비슷한 중간 질량이 나와서 "큐볼과 벽" 구분이 안 생긴다. 로그 균등은 **자연히 계급을 만든다.**
> v1.0의 `인접 질량비 >8 재샘플` 규칙은 삭제 권장 — 로그 균등이면 이미 섞인다.

**STEP 2 — 궤도 반경 (상호 힐 반경 간격, 닫힌 해)**
```
h_ij = ( (μ_i + μ_j) / (3·μ★) )^(1/3)          # 무차원 힐 계수
a₁   = a_min · U(1.0, 1.1)
a_{i+1} = a_i · (1 + K_i·h/2) / (1 − K_i·h/2)   # a_{i+1}−a_i = K·h·(a_i+a_{i+1})/2 의 닫힌 해
```
**오버플로 해소 루프**: `a_N > a_max`이면
```
while a_N > a_max (최대 2회):        # v1.0 8회 → 2회로 축소
    if mean(K) > K_lo + 0.3:  K_i ← max(K_lo, 0.9·K_i)   # 먼저 간격을 조인다
    else:                     μ_i ← 0.85·μ_i             # 그래도 넘치면 다이어트
    반경 재계산
실패 → N ← N−1 후 STEP 1부터
```
> **이 한 줄이 하는 일**: 무거운 성계일수록 자동으로 성기게, 가벼우면 촘촘하게 배치된다. "질량에 맞는 간격"을 손으로 튜닝할 필요가 없어진다. **닫힌 해라서 반복 없이 O(N)**이고, 이게 이 수식을 남긴 유일한 이유다.

**STEP 3 — 궤도 요소 → 상태 벡터**
```
e_i ~ Rayleigh(σ_e) clamp 0.25
ω_i ~ U(0, 2π)                  # 근일점 방향
ν_i ~ U(0, 2π)                  # 진근점각(초기 위상)
   → 초기 쌍거리 < 3·상호힐반경 이면 ν 재샘플 (최대 10회)

r  = a(1−e²) / (1 + e·cos ν)
h  = √(μ★·a·(1−e²))
φ  = ω + ν
pos = r·(cos φ, sin φ)
v_r = (μ★/h)·e·sin ν            # 지름 방향 성분
v_t = (μ★/h)·(1 + e·cos ν)      # 접선 방향 성분
vel = v_r·r̂ + v_t·t̂,  t̂ = (−sin φ, cos φ)   # 순행 기준
```
> **이건 교과서 공식이고 협상 불가다.** 위치·속도를 대충 찍으면 행성이 첫 10초 만에 궤도를 이탈한다. 케플러 요소로 생성해야 "일단 안정된 성계"에서 출발한다.

**STEP 4 — 바이옴 배정** (정규화 반경 `x = a_i / a_max`)

| 존 | x 범위 | 확률 분포 |
|---|---|---|
| 내열권 | < 0.30 | 용암 55 / 암석 30 / 독성 15 |
| 온대권 | 0.30~0.62 | 암석 30 / 해양 30 / 독성 20 / 용암 10 / 생명 10 |
| 외한권 | > 0.62 | 얼음 45 / 가스 35 / 암석 15 / 해양 5 |

> 순수 연출용처럼 보이지만 **합성 목표(§11)의 재료 공급**을 결정한다. 얼음이 외곽에만 나오므로 "얼음+용암" 레시피는 자동으로 *긴 거리를 밀어야 하는* 과제가 된다. 바이옴 표 하나가 레벨 디자인을 하고 있다.

**STEP 5 — 지구 & 목표 배치**
```
지구 슬롯 = argmin_i | a_i − 0.42·a_max |   # 해당 궤도의 행성을 지구로 치환 (μ=300)
목표 후보 = 목표 유형(§8)별 필터 통과 행성
차폐도 B(t) 계산 → §7.5
```

**STEP 6 — 안정성 사전 시뮬**
```
100 s를 dt=1/40 조립도로 돌려서:
  · 행성끼리 d < 0.8(R_i+R_j)  → 리젝트 (개장 전 충돌)
  · r > 2.6·a_max (이탈) 또는 r < R★+15 (태양 낙하) → 리젝트
예외: A≥5에서 12% 확률 "카오스 계" 모디파이어 — 위 리젝트를 면제하고 보상 +25% ⟨P2⟩
```
> **왜 100초인가**: 스테이지 최대 체류 시간(≈2~4분)보다 짧지만, 판이 "저절로 무너지는" 케이스는 대부분 초반에 드러난다. 100초를 넘기면 나머지는 플레이어가 흔들어서 무너뜨리는 것이고, 그건 **버그가 아니라 게임**이다.

### 7.4 구현 — 배치 생성기 (TypeScript)

```ts
export function placeOrbits(rng: Xoshiro, mus: number[], K: number[], aMin: number, aMax: number) {
  const n = mus.length;
  const axes = new Array<number>(n);

  const build = () => {
    axes[0] = aMin * (1 + rng.next() * 0.1);
    for (let i = 0; i < n - 1; i++) {
      const h = Math.cbrt((mus[i] + mus[i + 1]) / (3 * CFG.MU_STAR));  // 무차원 힐 계수
      const f = (K[i] * h) / 2;
      axes[i + 1] = axes[i] * (1 + f) / (1 - f);                        // 닫힌 해
    }
  };

  build();
  for (let guard = 0; guard < 2 && axes[n - 1] > aMax; guard++) {       // 오버플로 해소
    const kMean = K.reduce((a, b) => a + b, 0) / n;
    if (kMean > Math.min(...K) + 0.3) for (let i = 0; i < n; i++) K[i] = Math.max(3.6, K[i] * 0.9);
    else                              for (let i = 0; i < n; i++) mus[i] *= 0.85;
    build();
  }
  return axes[n - 1] <= aMax ? axes : null;    // null이면 N−1 후 재시도
}

export function elementsToState(a: number, e: number, w: number, nu: number) {
  const r   = (a * (1 - e * e)) / (1 + e * Math.cos(nu));
  const h   = Math.sqrt(CFG.MU_STAR * a * (1 - e * e));
  const phi = w + nu;
  const cp = Math.cos(phi), sp = Math.sin(phi);
  const vr = (CFG.MU_STAR / h) * e * Math.sin(nu);
  const vt = (CFG.MU_STAR / h) * (1 + e * Math.cos(nu));
  return {
    pos: { x: r * cp,             y: r * sp },
    vel: { x: vr * cp - vt * sp,  y: vr * sp + vt * cp },
  };
}
```

### 7.5 차폐도 B — "직사 금지"의 유일한 강제 장치  ⟨v2.0 승격⟩

v1.0에서 STEP 5의 한 줄이었지만, **P1(직사 금지)을 코드로 구현하는 건 이것뿐**이므로 독립 절로 올린다.

```
회랑(corridor) = 발사점(지구) → 목표 행성 을 잇는 폭 2·R_target 의 직선 띠
B(t) = 시각 t에서 그 회랑과 교차하는 "목표 아닌" 행성의 수
B    = mean( B(t) ),  t ∈ [0, 20s] 를 0.5s 간격으로 샘플

합격 조건:   0.6 ≤ B ≤ 2.5
```

| B 값 | 판의 성격 | 판정 |
|---|---|---|
| 0.0 | 항상 뻥 뚫림 → 조준선 긋고 쏘면 끝 | ❌ **게임이 아님** |
| 0.6 | 평균적으로 60% 시간 동안 뭔가가 막고 있음 | ✅ 하한 |
| 1.5 | 늘 한두 개가 가로막음. 우회 필수 | ✅ 이상적 |
| 2.5 | 벽이 겹겹 | ✅ 상한 |
| 4.0+ | 어떤 각도로도 답이 없음 | ❌ **상한 필요** ⟵ v1.0 누락 |

> **v1.0의 구멍**: 하한만 있고 상한이 없었다. 그러면 "완전히 막힌 판"이 통과할 수 있고, 솔버가 그걸 걸러내긴 하지만 **솔버를 25번 돌린 뒤에야** 걸러낸다. 상한 2.5를 먼저 걸면 그 25회 낭비가 사라진다. **싼 필터를 비싼 필터 앞에 두는 것.**

```ts
export function occlusion(from: Vec2, target: Body, others: Body[], eph: Ephemeris): number {
  let sum = 0, samples = 0;
  for (let t = 0; t <= 20; t += 0.5) {
    const tp = eph.posAt(target, t);
    const dx = tp.x - from.x, dy = tp.y - from.y;
    const len = Math.hypot(dx, dy), ux = dx / len, uy = dy / len;
    const halfWidth = target.radius;
    let count = 0;
    for (const o of others) {
      if (o === target || !o.alive) continue;
      const op = eph.posAt(o, t);
      const px = op.x - from.x, py = op.y - from.y;
      const proj = px * ux + py * uy;                       // 회랑 축 방향 투영
      if (proj <= 0 || proj >= len) continue;               // 목표 뒤/발사점 뒤는 제외
      const perp = Math.abs(px * -uy + py * ux);            // 축과의 수직 거리
      if (perp < halfWidth + o.radius) count++;
    }
    sum += count; samples++;
  }
  return sum / samples;
}
```

### 7.6 STEP 7 — 솔버 검증 (풀 수 있는 판인가?)

```
행성 궤적을 60 s 분량 사전 적분(에페메리스, dt=1/30) — 미사일은 시험입자라 재사용 가능
각 시도 25 s 시뮬. "해"의 정의:
  ① 목표 직격  ② 스윙바이 ≥ 1 (목표 외 행성에서 δ ≥ 25°, 무접촉 통과)
  ③ 파울 0  ④ 지구 피해 0
해율 = 해 수 / 시도 수
```

**v1.0 목표 해율 밴드(원본 유지 — 텔레메트리 목표치로 사용)**

| 안테 | 목표 해율 밴드 |
|---|---|
| 1 | 2.0 ~ 8.0 % |
| 2 | 1.2 ~ 5.0 % |
| 3 | 0.8 ~ 3.5 % |
| 4 | 0.5 ~ 2.5 % |
| 5 | 0.3 ~ 1.8 % |
| 6 | 0.2 ~ 1.2 % |
| 7 | 0.12 ~ 0.9 % |
| 8 | 0.08 ~ 0.6 % |

```
불합격 시 변이(순서대로, 총 25회 한도):
  ① 목표 행성 교체 → ② ν 전체 리롤 → ③ e 재샘플 → ④ 풀 리제네레이트
25회 소진 → 수제 템플릿 뱅크(20판)에서 뽑아 파라미터 지터 ±10%
```
- 궤도 시프트형 목표는 해 정의를 달리한다: "지정 큐 행성 3R_p 이내 통과 & 무파울" 궤적 ≥ 1 존재 **그리고** `로켓 수 × 타당 최대 Δa ≥ 목표 밴드 이동거리 × 1.5` (Δa 예산 검사).

### 7.7 솔버 성능 — v1.0 사양의 실제 문제와 수정안  ⟨v2.0 신규⟩

**v1.0은 "수백 ms"라고 썼지만, 그건 1회 비용이다. 변이가 25회까지 가면 최악 10초 히치다.**

```
4032 시도 × 750 스텝 × (태양 1 + 행성 ~6) ≈ 2,100만 힘 계산 ≈ 300~600 ms (JS 기준)
× 변이 25회 = 7.5 ~ 15 초                    ← 로딩 화면이 멈춘다
```

**수정안 A — 2단계 적응형 샘플링**

```
1차 스크리닝: 각도 24(15°) × 파워 4 × 시각 4 = 384 시도    (~30 ms)
  hits ≥ 8  → 해율 = hits/384 로 확정 (표본오차 ±36%, 밴드 3배 차이면 충분히 구분됨)
  hits ≤ 3  → 저해율 구간. 2차로 승격
2차 정밀:   각도 72(5°) × 파워 7 × 시각 8 = 4032 시도       (~400 ms, 드물게만)
```

**수정안 B — 해율 밴드 3단 축소** (구현용. 위 8단 표는 목표치로 보존)

| 구간 | 밴드 | 필요 표본 |
|---|---|---|
| A1–3 (도입) | 1.5 ~ 8 % | 384 |
| A4–6 (본편) | 0.5 ~ 3 % | 384 → 경계면만 1536 |
| A7–8 (종반) | 0.15 ~ 1.2 % | 1536 기본 |

> **왜 축소하는가**: 안테 8의 `0.08~0.6%`를 384 표본으로 재면 기댓값이 0.3~2.3발이다. **측정 자체가 불가능한 정밀도를 스펙에 적어두면 코드가 거짓말을 하게 된다.** 8단 표는 텔레메트리로 실측한 뒤 되살린다.

**수정안 C — 비동기 선생성**
상점 화면(플레이어가 5~15초 머무름)에서 **다음 스테이지를 미리 생성**한다. 이러면 솔버 비용이 사실상 0이 된다. 워커 스레드 1개면 충분.

> **난이도의 진짜 축은 해율이 아니다.** 해율은 결과 지표일 뿐이고, 플레이어가 체감하는 난이도는 ① 목표 유형(섬멸 < 합성 < 궤도시프트), ② 차폐도 B, ③ 행성 수와 K다. 해율 밴드는 **"너무 쉽거나 불가능한 판을 거르는 안전망"**으로만 쓰고, 난이도 설계는 §7.2 표로 한다.

### 7.8 소멸·이탈 경계 (런타임 공통 규칙)

```
미사일: r < R★+8  → 태양 소멸 / r > 2.8·a_max → 유실
행성:   r < R★+15 → "항성 처분" (섬멸로 인정 + 스타일 대량 점수)
        r > 2.6·a_max → "행성 추방" (섬멸로 인정, 점수 50%)
```
행성을 태양에 밀어 넣거나 성계 밖으로 걷어차는 것도 **정식 킬**이다. 당구로 치면 뱅크샷. 추방이 점수 50%인 건 "미는 게 부수는 것보다 쉽기 때문" — 난이도와 보상을 맞춘 것.

### 7.9 의사코드 (전체 파이프라인)

```python
def generate_stage(seed, ante, goal_type):
    rng = Xoshiro(seed)
    for attempt in range(25):
        mus   = sample_masses(rng, ante)            # STEP 1  로그균등
        axes  = place_orbits(rng, mus, ante)        # STEP 2  닫힌 해 + 오버플로
        if axes is None: continue
        elems = sample_elements(rng, axes, ante)    # STEP 3  e,ω,ν → 상태벡터
        biome = assign_biomes(axes)                 # STEP 4
        earth, target = place_earth_target(elems, goal_type)   # STEP 5

        if not stability_presim(elems, 100.0):      # STEP 6  싼 필터 먼저
            continue
        eph = integrate_ephemeris(elems, 60.0, dt=1/30)
        B = occlusion(earth.pos, target, elems, eph)           # §7.5  싼 필터
        if not (0.6 <= B <= 2.5):
            continue
        rate = solver_rate_adaptive(eph, earth, target, goal_type)  # §7.7 비싼 필터
        if in_band(rate, ante):
            return Stage(elems, biome, earth, target, seed, B, rate)
        mutate_in_place(rng, elems, attempt)        # 목표교체→ν→e→풀리롤
    return template_bank.pick(rng, ante).jitter(0.10)

def step(bodies, missiles, dt=1/120):
    kick(bodies, dt/2); drift(bodies, dt); kick(bodies, dt/2)   # KDK, ε=3
    for m in missiles:
        sub = 4 if near_planet(m, 5.0) else 1
        for _ in range(sub):
            a = grav(m, star, k=1.0) + sum(grav(m, p, k=KAPPA) for p in planets)
            integrate(m, a, dt/sub)
        update_encounters(m)      # 스윙바이/니어미스/포획 판정 (§5.2)
        resolve_hits(m)           # 데미지·임펄스·파울 (§5.3, §6)
```

**필터 순서가 중요하다**: 안정성(싼 것) → 차폐도(싼 것) → 솔버(비싼 것). v1.0은 솔버가 두 번째였다.

---

## 8. 목표(블라인드) 설계

| 슬롯 | 유형 | 클리어 조건 | 내러티브 축 | 비고 |
|---|---|---|---|---|
| 스몰 | 섬멸 | 목표 행성 HP 0 (태양 처분·추방 인정) | ① 조르그 응징 | 차폐도 0.6≤B≤2.5 보장 |
| 빅 | 궤도 시프트 | 지정 행성(주로 지구)의 a를 목표 밴드 `[a₀+40, a₀+90]`에 넣고 **5초 유지** | ② 태양 회피 | 지구 직격 금지 → 당구·중력 끌기·특수탄으로 |
| 빅(변형) | 합성 | 지정 레시피 행성 생성 (예: 얼음+용암→암석) | ③ 신규 행성 | v < v_shatter 유지가 관건 |
| 보스 | 기믹 보스 | 아래 표 | ①②③ 혼합 | 보스판은 수제 템플릿 + 지터 |

> **세 유형이 서로 다른 스킬을 요구한다.** 섬멸은 *조준*, 궤도 시프트는 *누적 계획*(로켓 4발의 Δa 예산 배분), 합성은 *속도 조절*(때리되 살살). 그래서 안테 하나 안에서 3판이 지루하지 않다.
> **"5초 유지" 조건의 이유**: 순간적으로 밴드에 스치는 걸로 클리어되면 운빨이 된다. 5초 유지를 요구하면 **궤도를 안정적으로 옮겨야** 하고, 그건 계획된 플레이의 결과다.
>
> ⚠️ **밸런스 지적(v2.0)**: 밴드 폭이 50 GU(`a₀+40 ~ a₀+90`)인데 표준탄 1타의 Δa는 73~246 GU다. **접선 직격 한 방이면 밴드를 그냥 지나친다.** 이건 버그가 아니라 §6.2의 `Δv·cos θ` 각도 제어를 강제하는 구조지만, **플레이어가 그걸 모르면 그냥 불공정한 판**이다. 대응 둘 중 하나:
> ① 밴드를 `a₀+60 ~ a₀+160`로 넓힌다(권장, 안전).
> ② 폭을 유지하되 §14.3에 **"이 각도로 때리면 Δa 얼마"** 예측 링을 조준 중 상시 표시한다(권장, 재미있음).
> 수직 슬라이스에서 ②를 먼저 시험한다.

**보스 6종**

| 보스 | 기믹 | 강제하는 기술 |
|---|---|---|
| 조르그 관세청 (요새 행성) | 실드: `v_imp ≥ 90`만 유효 | 스윙바이 **가속** |
| 유령 위성 몬드 | 8초 주기로 위상 소멸/실체화, 실체 때만 피격 | 발사 **타이밍** |
| 쌍성 브라더즈 | 태양 2개(μ 1.55e5×2, 간격 320 상호 공전). 행성은 외곽 P형 궤도만. 중력장이 8자로 출렁임 | 비정상 중력장 **독해** |
| 헌병 함대 | 직선 비행 2.5초 초과한 미사일 요격 | **연속 굴절** (쉬지 않고 휘어야 생존) |
| 블랙홀 브로커 | μ★ ×2.2 + 강착원반. 근일점 120 이내 통과 샷 배율 ×2 | **오베르트** 리스크 감수 |
| **조르그 프라임 (최종)** | HP 30, 3페이즈 실드: P1 `v≥100`만 / P2 스윙바이 3체인 이상 탄만 / P3 **미사일 면역, 행성 당구 직격만 유효** | 전부 |

> **최종보스 P3가 이 게임의 논지다.** 미사일이 안 통하고 오직 *행성을 쳐서 행성으로* 잡아야 한다 — 게임 제목이 PlanetPool인 이유를 마지막에 회수한다. 이 페이즈를 위해 앞의 모든 시스템(임펄스, 합성, N체)이 존재한다.

---

## 9. 스코어 · 경제 · 상점

### 9.1 점수
```
샷 점수 = (명중 위력 합 P) × 배율 M
M = 1 + 0.75·스윙바이체인 + 0.25·니어미스 수 + 0.5·태양 가속(근일점<120 통과 시)
```
- 3체인 이상 = **"스리쿠션"** 연출(잔상 + 샷 리플레이 슬로모).
- 스테이지 랭크 S/A/B/C = 점수/기준치.

> **배율이 가산인 이유**: 발라트로가 곱배율로 폭발하는 건 조커 5개가 곱해지기 때문이다. 여기선 배율원이 *한 샷 안에서* 나오므로(체인·니어미스), 곱하면 4체인에 1000배가 된다. 가산 0.75/체인은 3체인에 M=3.25 — **"두세 배 잘 친 샷"이라는 체감과 맞는다.** 곱배율은 유물(§10.2) 쪽에 둔다.

### 9.2 연구비 ($)
```
클리어 4$ + 남은 로켓 ×1$ + 노파울 1$ + 신속작전(90초 내) 1$ + 랭크 보너스(S:2, A:1)
```
> **"남은 로켓 ×1$"이 P2(기다리는 것이 리롤)와 긴장 관계**를 만든다. 오래 기다려서 완벽한 정렬을 잡으면 로켓을 아끼지만 신속 보너스를 잃는다. 두 보상이 서로를 견제하도록 설계됨.

### 9.3 상점 (스테이지 사이)
- 슬롯: 탄종 2 / 유물 2 / 소모품 1 / 랜덤 팩 1(3택1). 리롤 2$부터 +1$씩.
- 가격대: 탄종 4~7$, 유물 5~8$, 소모품 2~4$, 팩 4$.
- 판매 = 구매가 절반.
- **상점 진입 시 다음 스테이지 비동기 생성 시작** (§7.7-C).

---

## 10. 로그라이크 업그레이드

내러티브 포장: 전부 **"지구 과학의 발전"**. 상점 = 국제공동연구예산 심사. 플레이어가 사는 건 무기가 아니라 *논문*이다.

### 10.1 탄종 (Peglin의 오브 포지션 — 발사 전 슬롯에서 선택)
| 탄종 | 위력 | 효과 |
|---|---|---|
| 표준 ICBM-Ω | 10 | 기본탄. 시작 시 4발 전부 이것 |
| MIRV | 4×3 | 수동 분열 키 — 원하는 순간 3갈래 15° 산개 |
| 벙커버스터 | 8 | 첫 충돌을 관통(데미지 주고 계속 비행). 파울 판정도 1회 면제 |
| 라그랑주 앵커 | 3 | 착탄점에 5초 임시 중력 우물(κ·μ=120,000) 생성 — 행성을 *끌어당긴다* |
| 이온 세일 | 7 | 비행 중 초당 +3 가속. 장거리·후반 가속용 |
| 역추진 패키지 | 9 | 비행 중 1회 ±30° 수동 보정 |
| 데코이 클러스터 | 1×5 | 5발 산탄, 스윙바이 체인 판정 공유 — 배율 셋업용 |
| 반물질 탄두 | 40 | 발사 반동으로 **지구가 Δv 1.5 밀림** (리스크이자 궤도조정 수단) |
| 중력자 어뢰 | 5 | 유일하게 질량 보유(κ·μ=48,000) — 지나가며 행성을 살짝 끈다. 정밀 궤도 시프트용 |
| 태양 다이버 | 12 | 근일점 120 이내 통과 시 위력 ×3 (오베르트 컨셉) |

> **탄종 설계 원칙**: 위력이 높을수록 조작 자유도가 낮다. 반물질(40)은 지구를 밀어버리고, 라그랑주 앵커(3)는 거의 데미지가 없는 대신 **판 자체를 조작**한다. "세게 vs 영리하게"의 축.

### 10.2 유물 (조커 포지션, 패시브 5슬롯)
| 유물 | 효과 |
|---|---|
| 뉴턴의 사과 | 스윙바이 배율 0.75 → 1.25 |
| 케플러의 컴퍼스 | 예측선 3s → 6s |
| 라플라스의 악마 | 예측선이 행성 이동·섭동까지 풀 시뮬 반영 |
| 오컴의 면도날 | 매 스테이지 행성 −1, 점수 +25% |
| 치올콥스키의 유산 | 로켓 +1/스테이지 |
| 다이슨 견적서 | **μ★ −10%** (전 궤도가 느슨해짐 — 물리 전역 변조) |
| 마흐의 원리 | κ +25% (미사일이 더 잘 휨) |
| 카이퍼 보험 | 스테이지당 첫 파울 무효 |
| 오르트 구름 계약 | 스테이지 시작 시 혜성 1개 스폰 (추가 큐볼, LEGAL_HIT 취급) |
| 로슈의 한계 | 니어미스만으로 표면 데미지 3 (스치면 긁힌다) |
| 판스퍼미아 | 합성으로 생명 행성 탄생 시 영구 배율 +0.5 (스택) |
| 폰 브라운의 노트 | 상점 탄종 반값 |
| 상대성 보정 | 관제실 모드 슬로모 0.25× 추가 (조준 중 초저속 재생) |
| 갈릴레이의 오기 | 역행 행성 존재 시 모든 점수 ×1.5 |

> **유물 3종은 물리 상수 자체를 만진다** — `다이슨 견적서(μ★)`, `마흐의 원리(κ)`, `오컴의 면도날(N)`. 이게 이 게임 유물의 정체성: 발라트로 조커가 점수 파이프라인을 만진다면, 여기선 **세계의 법칙을 만진다.** 획득 순간 판이 눈에 띄게 달라 보여야 한다(모든 궤도 트레일이 한 번 리드로우되는 연출).

### 10.3 소모품 (타로 포지션, 2슬롯)
| 소모품 | 효과 |
|---|---|
| 성간 스캐너 | 이번 스테이지 전 천체의 30초 미래 고스트 표시 |
| 위상 재배열 | 전 행성 진근점각 ν 리롤 (판 셔플) |
| 조석 펄스 | 지정 행성에 즉시 Δv 4 (방향 선택) — 로켓 안 쓰는 궤도 조정 |
| 휴전 협정 | 이번 샷 파울 규칙 해제 |
| 응급 예산 | 이번 스테이지 로켓 +2 |
| 관측 위성 | 차폐도·해율 힌트 표시 (판의 난이도를 보여줌) |

---

## 11. 행성 타입 & 합성표

타입: 암석 / 용암 / 얼음 / 해양 / 가스 / 독성 / 생명 / 지구 / 파편

| 합성 (v < v_shatter) | 결과 | 부가 효과 |
|---|---|---|
| 얼음 + 용암 | 암석 | **수증기 폭발**: 반경 300 내 전 행성에 바깥 방향 Δv 3 |
| 해양 + 용암 | 암석(온천) | 점수 보너스 ×2 |
| 암석 + 암석 | 철 행성 | HP ×2, 질량 합산 — 최고의 당구공 |
| 가스 + 아무거나 | 가스(성장) | 상대를 흡수, μ·R 증가 |
| 독성 + 해양 | **생명 행성** | 대량 점수 + (판스퍼미아 트리거) |
| 얼음 + 얼음 | 혜성 파편 ×6 | 소형 큐볼 다수 |
| 생명 + 아무거나 | 파쇄 강제 | 외교 게이지 +1 ("그건 좀…") |

> **"얼음+용암 → 수증기 폭발"이 합성의 킬러 무브다.** 결과물(암석)보다 부가 효과(반경 300 전방위 Δv 3)가 판을 뒤집는다. 궤도 시프트 목표에서 **로켓 0발로 지구를 미는 콤보**가 여기서 나온다 — 플레이어가 스스로 발견했을 때 가장 기분 좋은 종류의 상호작용.
> **"생명 + 아무거나 → 파쇄 강제 + 외교 −1"**: 게임이 도덕적 판단을 한 줄 농담으로 처리한다. 이 톤이 전체 작품의 목소리다.

---

## 12. 인트로 시나리오 & 스토리

### 12.1 오프닝 컷씬 (레트로 스타일)

연출 지침: 흑백 성계도 배경, 스캔라인, 타자기 텍스트 + 비프음. 닌자가이덴식 극적 클로즈업 3컷 — ① 수은이 끓어 넘치는 온도계 ② 박사 선글라스에 반사된 큐대 ③ 팩스기.

```
서기 2199년.
태양 출력 +3%.
여름 최고기온 61℃.
에어컨 회사 주가 상한가.

인류 비상 회의. (소요 시간: 3분)

의장: "해결책은?"
박사: "지구를 뒤로 뺍니다."
의장: "어떻게?"
박사: "당구식으로."
의장: "……뭘로 치는데?"
박사: "핵."
의장: "승인."

[도장 쾅. 서류 제목: 『PLANETPOOL 계획』]

그날 오후, 이웃 항성계
조르그 제국에서 팩스가 도착했다.

  "너희 태양, 우리가 데워 봤다.
   어떠냐. — 조르그"

박사는 말없이 큐대에 초크를 발랐다.

"……겸사겸사 칩시다."

        P L A N E T P O O L
           PRESS START
```

→ 인트로 하나로 목표 ①(전쟁: 조르그 응징) ②(태양 이슈: 지구 궤도 후퇴)가 전부 정당화된다. ③(합성)은 액트 중 박사의 통신으로: **"부수다 보니 만들 수도 있겠는데요."**

> **톤 규칙**: 관료제 코미디 + 무표정한 과학자. 절대 비장해지지 않는다. 지구가 멸망 직전인데 회의는 3분이고 에어컨 주가가 먼저 나온다. **이 온도차가 유일한 개그 엔진**이므로 모든 텍스트가 이걸 지킨다.

### 12.2 액트 구성 (안테 매핑)
| 액트 | 안테 | 내용 | 막간 한 줄 |
|---|---|---|---|
| 1. 태양계 재개발 | 1–2 | 연습 겸 동네 청소, 지구 궤도 1차 후퇴 | 조르그 팩스 2탄: "빗나가더라 ㅋ" |
| 2. 국경 분쟁 | 3–4 | 조르그 식민 성계. 보스: 조르그 관세청 | 관세청 전언: "핵미사일은 과세 대상입니다" |
| 3. 본성 진공 | 5–6 | 수도 성계. 보스: 조르그 프라임 1차전 | 조르그 황제: "당구는 신사의 스포츠다. 핵은 반칙." — 박사: "마세이도 반칙임?" |
| 4. 대이주 | 7–8 | 데워진 태양이 진짜 맛이 감. 진최종 "브레이크 샷": 새 항성계 골 밴드에 지구 포함 3행성 안착 | — |

### 12.3 엔딩
```
지구, 신궤도 안착.
평균기온 18℃. 에어컨 회사 파산.
박사는 은퇴 후 당구장을 차렸다.
간판: 「구장(球場) — 행성은 사절」

[크레딧 후]
안드로메다發 팩스 수신.
  "재밌어 보이던데. 우리도 껴도 됨?"
```

### 12.4 텍스트 전달 방식 (구현 사양)
- 전부 **팩스 수신** 형식. 로딩 화면과 스테이지 사이에 1장씩 출력되는 종이.
- 타자기 효과 40cps, 스킵 가능. 총 텍스트량 목표: **런 1회당 400자 이하.**
- 컷씬은 전부 텍스트 + 정지 이미지. 애니메이션 자산 0개로 만드는 게 목표.

---

## 13. 온보딩 & 티칭 커리큘럼  ⟨v2.0 신규⟩

**이 게임의 가장 큰 리스크는 "첫 5분에 스윙바이를 못 하고 나가는 것"이다.** 튜토리얼을 별도로 만들지 않고, **안테 1의 PCG 파라미터를 조작해서** 가르친다.

| 판 | 가르치는 것 | PCG 강제 조건 |
|---|---|---|
| A1 스몰 | "쏘면 휜다" | 행성 3개. 목표와 발사점 사이 무거운 행성(μ≥400) 1개 고정 배치. B ≈ 1.0. **직사하면 그 행성에 맞고 파울** → 실패로 배운다 |
| A1 빅 | "기다리면 판이 바뀐다" | 발사 직후 15초는 목표가 완전 차폐(B=3), 20초 후 통로가 열림. **첫 샷을 낭비하게 두고, 시계 아이콘을 강조** |
| A1 보스 | "체인이 점수다" | 2체인이 가능한 배치를 강제(솔버가 δ≥25° 2회 해를 찾을 때까지 재생성). 랭크 기준치를 1체인으로는 못 넘게 설정 |
| A2 스몰 | "무거운 놈은 안 밀린다" | 목표를 μ≥700로 고정. 표준탄 4발로는 못 밀고 **때려야** 함 |
| A2 빅 | "행성으로 행성을 친다" | 궤도 시프트 첫 등장. 지구 옆에 μ150 큐볼 후보를 명시적으로 하이라이트 |

- **첫 사망까지 튜토리얼 텍스트는 3줄 이하.** 나머지는 실패 태그(§14.4)가 가르친다.
- A1은 외교 게이지 페널티를 **경고만** 하고 카운트하지 않는다(숨은 유예). A2부터 실제 적용.

---

## 14. 정보 설계 · UI / 연출

**N체 게임에서 UI는 편의가 아니라 코어 메카닉이다.** "20초 뒤 저 행성이 어디 있는가"를 못 읽으면 이 게임은 플레이가 불가능하다.

### 14.1 카메라
- 성계 전체 뷰(a_max×1.15 핏)가 기본. 미사일 발사 시 PIP 추적캠(우상단).
- 휠 줌 / 우클릭 드래그 패닝. 스윙바이 순간 자동으로 해당 행성 쪽으로 약간 리드.

### 14.2 궤도 트레일 (P4의 구현체)
- 전 행성 **최근 20초 잔상선** — *궤도가 변하고 있음*을 보여주는 핵심 가시화.
- 플레이어 임펄스 직후 해당 행성의 트레일만 **1.5초간 색 강조 + 굵기 2배.** "저건 내가 민 거다"를 못 박는다.
- 유물 획득 시 μ★나 κ가 바뀌면 **전 트레일 리드로우 플래시** — 세계가 바뀌었음을 알린다.

### 14.3 미래 예측 3단계 (정보를 파워업으로 판다)
| 단계 | 획득 | 보이는 것 |
|---|---|---|
| 기본 | — | 미사일 예측선 3초, "현재 중력장 프리즈" 근사 |
| 케플러의 컴퍼스 | 유물 | 예측선 6초 |
| 라플라스의 악마 | 유물 | 행성 이동·섭동 반영 풀 시뮬 |
| 성간 스캐너 | 소모품 | 전 행성의 30초 미래 고스트(반투명 잔상) |

### 14.4 실패 피드백 — 왜 빗나갔는가  ⟨v2.0 신규, P5⟩

미사일이 소멸할 때마다 **원인 태그를 정확히 1개** 화면 중앙에 띄운다(1.2초, 앰버색).

| 태그 | 판정 조건 | 플레이어가 배우는 것 |
|---|---|---|
| `굴절 부족 — 18° / 25° 필요` | 최대 굴절이 문턱 미달 | 더 느리게, 더 가깝게 |
| `속도 초과 — 통과만 함` | 인카운터 진입했으나 δ<10° | 파워를 낮춰라 |
| `포획됨` | wrap ≥ 0.75 | 너무 느렸다 |
| `타이밍 — 목표가 4.2초 뒤 지남` | 목표 최근접 시각과의 오차 | 발사 시각을 늦춰라 |
| `파울 — 조르그령 침범` | LEGAL_HIT 외 명중 | 회랑을 다시 봐라 |
| `유실 — 성계 이탈` | r > 2.8·a_max | 파워 과다 |

추가로 **빗나간 샷의 궤적은 회색 점선으로 스테이지 끝까지 남긴다.** 다음 샷을 그 옆에 놓고 비교 조준할 수 있게 — 이게 이 게임의 "조준 보조"다.

### 14.5 타격감 연출
- 스윙바이 성립 순간: 링 플래시 + 피치 상승 SFX + 체인 카운터 팝.
- 명중: 히트스탑 0.15s → 스크린셰이크 → 크레이터 데칼 → 임펄스로 밀리는 궤도 트레일 변형.
- 3체인 이상 클리어 샷: 자동 리플레이 슬로모("스리쿠션 캠").
- 행성 파괴: 화면 전체 화이트 플래시 0.1s → 파편 산개 → 저역 임팩트.

---

## 15. 사운드 브리프  ⟨v2.0 신규⟩

레트로 CRT 컨셉에 맞춰 **전 사운드를 신스 합성으로** 생성(Tone.js 등). 오디오 자산 파일 0개가 목표.

| 이벤트 | 소리 | 목적 |
|---|---|---|
| 조준 중 | 저역 사인 드론 (60Hz), 파워에 따라 피치 ↑ | 파워를 귀로 읽게 |
| 발사 | 화이트노이즈 버스트 + 하강 스윕 | 무게감 |
| 인카운터 진입 | 하모닉 링잉 시작(행성 μ에 비례한 피치) | **어느 행성 권역인지 귀로 구분** |
| 스윙바이 성립 | 완전5도 상승 아르페지오, 체인마다 한 옥타브 위 | 체인 = 음정 상승 = 쾌감 |
| 니어미스 | 짧은 도플러 스와이프 | 아슬아슬함 |
| 파울 | 부저 + 팩스 수신음 | 관료제 개그 |
| 행성 파괴 | 서브베이스 임팩트 + 노이즈 테일 3초 | 무게 |
| 합성 성립 | 상승 벨 코드 (장3화음) | 파괴와 정반대의 정서 |

> **체인이 음정으로 올라가는 게 핵심**이다. 플레이어는 화면을 안 봐도 "지금 3체인"임을 안다. 발라트로의 점수 상승음이 하는 역할.

---

## 16. 실패 조건 & 난이도

- **실패**: ① 로켓 소진(목표 미달) ② 지구 HP 0 ③ 외교 게이지 3.
- 스테이지 재도전 없음 — 런 종료 (발라트로 동일).
- 난이도 축(안테 스케일): 행성 수↑, K↓(더 카오스), e↑, 해율 밴드↓, 보스 기믹.
- **하드 모드**: 관제실 모드 강제 OFF(실시간 조준) + 예측선 1.5s.
- 메타 진행(후순위): "예산 등급" 스테이크 — 백색왜성 → 중성자성 → 블랙홀 (파울 페널티 강화, 상점 축소 등).

> **실패 3종이 각각 다른 플레이 실수를 처벌한다**: 로켓 소진 = 계획 실패, 지구 파괴 = 욕심(반물질 탄두), 외교 3 = 부주의. 셋이 겹치지 않아야 "왜 죽었는지"가 명확하다.

---

## 17. 구현 우선순위

| 단계 | 범위 |
|---|---|
| **P0 (수직 슬라이스)** | 물리 코어(태양+행성 리프프로그, κ 미사일, 서브스텝) / PCG §7 전체(목표는 섬멸만) / **차폐도 B** / 발사 UI+프리즈 예측선 / 스윙바이·파울·점수 / **실패 태그(§14.4)** / 탄종 3(표준·MIRV·역추진) / 유물 6 / 상점 최소 / 안테 3까지 런 루프 / **온보딩 A1~A2(§13)** |
| **P1** | 궤도 시프트·합성 목표 + 합성표 / 텍스처 데미지 4단계·파편 / 보스 3종 / 탄종·유물 풀셋 / 인트로 컷씬 / 사운드 브리프 전체 |
| **P2** | 보스 6종·카오스 계·역행 행성 / 하드 모드·스테이크 / 데일리 시드·리플레이 공유 / 밸런스 텔레메트리(스테이지별 실제 해율 로깅 → §7.6 8단 밴드 복원) |

> **P0에 차폐도·실패태그·온보딩을 올린 이유**: 이 셋이 없으면 수직 슬라이스가 "재미없다"는 결론을 잘못 내리게 된다. 물리가 잘 돌아도 *직사로 클리어되는 판*이 나오거나 *왜 빗나갔는지 모르면* 게임의 가치를 측정할 수 없다.

---

## 18. 리스크 & 플레이테스트 질문  ⟨v2.0 신규⟩

수직 슬라이스로 **반드시 확인해야 할 것**들. 여기서 답이 나쁘면 수식을 아무리 다듬어도 소용없다.

| # | 리스크 | 확인 질문 | 실패 시 대응 |
|---|---|---|---|
| R1 | **스윙바이가 안 일어남** | 신규 플레이어가 첫 10샷 안에 δ≥25°를 한 번이라도 성립시키는가? | κ를 60→80. 파워 상한을 140→110 |
| R2 | **판을 못 읽음** | "20초 뒤 저 행성이 어디 있을지" 물었을 때 대충이라도 맞히는가? | 성간 스캐너를 소모품이 아닌 기본 기능으로 |
| R3 | **N체가 그냥 노이즈로 보임** | 플레이어가 "궤도가 변하고 있다"를 자발적으로 언급하는가? | 트레일 길이 20s→40s, 자연 섭동 강도 상향 |
| R4 | **기다림이 지루함** | 4배속 스킵을 쓰는가, 아니면 그냥 아무렇게나 쏘는가? | 4배속을 8배속으로. 정렬 예고 UI 추가 |
| R5 | **궤도 시프트 목표가 불투명** | "지구를 얼마나 더 밀어야 하는지" 화면에서 읽히는가? | 목표 밴드를 궤도 위에 실제 링으로 렌더 |
| R6 | **파울이 부당하게 느껴짐** | 파울 후 "내 잘못"이라고 느끼는가, "억울"한가? | LEGAL_HIT 아닌 행성에 상시 붉은 외곽선 |
| R7 | **로딩 히치** | 스테이지 전환이 1초 이상 멈추는가? | §7.7-C 비동기 생성 강제 |

**튜닝 순서 권장**: κ → 스윙바이 문턱(25°) → 차폐도 밴드 → 해율 밴드 → 점수 배율. 앞의 것을 고치면 뒤가 전부 흔들리므로 역순으로 만지면 안 된다.

---

## 부록 A. 밸런스 상수표 (튜닝 원장)

| 상수 | 값 | 계층 | 비고 |
|---|---|---|---|
| μ★ / R★ | 3.0e5 / 40 | T1 | 태양 고정 |
| dt / ε | 1/120 s / 3 | T1 | 서브스텝 ×4 @ d<5R_p |
| **κ (당구감)** | **60** | **T1** | **범위 40–80. 최우선 튜닝 노브** |
| 미사일 파워 | 40–140 GU/s | T1 | 느림=휨, 빠름=셈 |
| R_p | 1.6·μ^(1/3) | T1 | 지구 μ300, R11 |
| HP | 12 + μ/12 | T1 | |
| 데미지 | P·(0.5+0.5·min(v/120,2))·(1+0.2체인) | T1 | ⚠️ 튜닝 후보: `/120` → `/90` (§6.1 지적) |
| 임펄스 | J=75P, Δv=J/μ_p, 방향 0.7v̂+0.3n̂ | T1 | |
| 파쇄 | 2.5·√(2Σμ/ΣR) | T1 | 미만이면 융합 |
| 스윙바이 | 존 6R_p / δ≥25° / 니어미스 1.8R_p / 포획 0.75바퀴 | T1 | |
| 배율 | 1 + 0.75·체인 + 0.25·니어미스 + 0.5·태양가속 | T1 | 가산 (곱은 유물에) |
| 경계 | 미사일 소멸 r<R★+8 / 유실 r>2.8a_max | T1 | |
| 경계 | 처분 r<R★+15 / 추방 r>2.6a_max | T1 | |
| 파편 | n=clamp(4+⌊μ/150⌋,4,9), 질량 55% | T1 | |
| a_min / a_max(A) | 260 / 1150+90·min(A,6) | T2 | |
| N(A) | 3+⌊(A+1)/2⌋ (+35%) cap8 | T2 | |
| μ_p 범위 | 로그U[80, 250+60A] | T2 | 목성형 10% ×3 cap2500 |
| K(A) | U(max(3.6, K_hi−2), 9−0.5A) | T2 | Gladman 3.46 상회 |
| σ_e(A) | 0.02+0.012A, clamp 0.25 | T2 | Rayleigh (U로 대체 가능) |
| **차폐도 B** | **0.6 ≤ B ≤ 2.5** | **T2** | **상한은 v2.0 신규** |
| 솔버 | 1차 384 → 2차 4032 적응형 | T2 | 밴드 §7.7-B, 변이 25회 → 템플릿 20 |
| 로켓/외교/지구HP | 4(보스5) / 3 / 3 | 규칙 | |

**T3 (코드 아님, 튜닝 계산기)**

| 도구 | 식 | 쓰는 순간 |
|---|---|---|
| 공전 속도/주기 | `v_c=√(μ★/a)`, `T=2πa/v_c` | 궤도 대역 정할 때 |
| 궤도 이동량 | `Δa ≈ 2a·Δv/v_c` | 임펄스 J 정할 때 |
| 안정 한계 | Gladman `Δ > 2√3 ≈ 3.46` | K 범위 정할 때 |
| **굴절각** | `δ = 2·arcsin(1/e)`, `e = 1 + r_p·v∞²/(κμ_p)` | **κ·파워·문턱 정할 때 (최다 사용)** |
| SOI 확대율 | `κ^(2/5) ≈ 5.1×` | 스윙바이 존 6R_p 근거 |

---

## 부록 B. 자료구조 정의 (TypeScript)

```ts
export type PlanetType = 'rock' | 'lava' | 'ice' | 'ocean' | 'gas' | 'toxic' | 'life' | 'earth' | 'debris';
export type GoalType   = 'annihilate' | 'orbitShift' | 'synthesis' | 'boss';

export interface Stage {
  seed: number;
  ante: number;                 // 1..8
  goal: Goal;
  bodies: Body[];
  earth: Body;
  rockets: number;              // 4 (보스 5)
  legalHits: Set<Body>;
  occlusion: number;            // 검증된 B 값 (관측 위성이 표시)
  solveRate: number;            // 검증된 해율
}

export interface Goal {
  type: GoalType;
  target?: Body;                            // annihilate / boss
  shiftBand?: { body: Body; lo: number; hi: number; holdSec: number };  // orbitShift
  recipe?: [PlanetType, PlanetType, PlanetType];                        // synthesis
}

export interface StageState {
  time: number;
  diplomacy: number;            // 0..3
  earthHp: number;              // 0..3
  rocketsLeft: number;
  score: number;
  shotScore: number;
  chain: number;
  fail(reason: 'OUT_OF_ROCKETS' | 'EARTH_LOST' | 'DIPLOMATIC_INCIDENT'): void;
  pushTrailHighlight(b: Body): void;
  emitFailTag(tag: FailTag, detail?: string): void;    // §14.4
}

export type FailTag =
  | 'DEFLECTION_SHORT' | 'TOO_FAST' | 'CAPTURED'
  | 'TIMING_MISS' | 'FOUL' | 'LOST_TO_SPACE';
```

## 부록 C. 통합 프레임 루프

```ts
export function tick(st: StageState, stage: Stage, missiles: Missile[], dtFrame: number) {
  let acc = dtFrame * st.timeScale;              // 1× 또는 4× (관측 스킵)
  while (acc >= CFG.DT) {
    stepBodies(stage.bodies, CFG.DT);            // §4.4  T1-1,2
    for (const m of missiles) {
      if (!m.alive) continue;
      stepMissile(m, stage.bodies, CFG.DT);      // §4.4  T1-3
      tracker.update(m, stage.bodies, b => onFoul(st, b));   // §5.4  T1-4
      checkContact(m, stage.bodies, stage.legalHits, st);    // §6.4  T1-5,6
      checkBounds(m, st, stage);                 // §7.8
    }
    resolveBodyPairs(stage.bodies, st);          // §6.4  T1-7
    checkBodyBounds(stage.bodies, st, stage);    // §7.8
    st.time += CFG.DT;
    acc -= CFG.DT;
  }
  evaluateGoal(st, stage);                       // §8  (궤도시프트 5초 유지 등)
}
```

---

> **끝.** 이 문서의 모든 숫자는 **부록 A 한 장에서만** 튜닝한다.
> 수식이 왜 있는지 헷갈리면 §0의 계층 태그(T1/T2/T3)를 보면 된다.
> 판단이 갈리면 §3의 설계 기둥 5개로 결정한다.
