// 기획서 부록 A — 밸런스 상수표. 모든 숫자는 이 파일에서만 튜닝한다.
export const CFG = {
  // 태양 반경 105 → 150. 판정 원(HIT_R)을 3.4로 키우면서 목성의 그려지는 원이
  // 111 GU가 되어 태양(105)보다 커 보였다 — 태양이 판의 중심으로 안 읽힌다.
  // 덤으로 "태양 포켓"이 넓어져 떨어뜨려 처분하는 선도 살아난다.
  DT: 1 / 120, EPS: 3, MU_STAR: 3e5, R_STAR: 150,
  // κ = 미사일이 받는 행성 중력 배율(§4.3). κ★ = 태양 배율.
  // 기획서 원본은 κ=60 / 태양 1.0이었으나 계측 결과 인카운터 평균 최대굴절이
  // 2.2°(문턱 25°)에 그쳐 스윙바이가 사실상 발생하지 않았다(§18 R1).
  // κ↑ + 태양↓ 로 평균굴절 55°까지 올린다. 행성끼리는 여전히 진짜 물리(κ=1).
  KAPPA: 120, KAPPA_STAR: 0.6,
  SUBSTEP_DIST: 5, SUBSTEP_N: 4,
  // 발사 속도 P_v (§5.1). 하한은 **지구의 κ증폭 탈출속도 위**여야 한다.
  // (구값 18은 KAPPA=60 시절의 계산이라 KAPPA=120인 지금은 지구 탈출 22.7보다
  //  낮았다 — 쏜 탄이 지구로 되떨어지는 자살 대역이 열려 있었다.)
  // 상한은 카이퍼 벨트가 튕겨 주므로 예전만큼 조심할 필요가 없다.
  LAUNCH_MIN: 26, LAUNCH_MAX: 56,
  // 발사 오프셋 — 지구 중력권 밖에서 쏜다. 구값(R_E+8=19)에서는 지구의
  // κ증폭 탈출속도가 43.5로 태양 탈출속도(36.5)보다 높아서
  // "지구를 벗어나면 반드시 성계도 벗어나는" 상태였다.
  // ※ 여기서 지구의 κ증폭 탈출속도는 escapeSpeed(EARTH_MU, LAUNCH_OFFSET) 로
  //   계산된다(아래). LAUNCH_MIN은 그 값 위에 있어야 한다 — 안 그러면 쏜 탄이
  //   지구 중력권을 못 벗어나고 되돌아와 자기 지구에 박힌다.
  LAUNCH_OFFSET: 140,
  MISSILE_TTL: 52,                    // 미사일 자폭(초). 성계가 넓어진 만큼 늘렸다
  SWING_ZONE: 6, SWING_DEG: 25, NEAR_MISS: 2.8, CAPTURE_WRAP: 0.75,   // §5.2
  // 당구공 반경 배율. 이 게임에는 이제 **원이 하나뿐이다**:
  // 그리는 원 = 미사일이 맞는 원 = 행성끼리 부딪히는 원.
  // 1.6이던 값을 올린 건 계측 결과다(§18 R2): 궤도 간격이 240 GU인데 공 반경이
  // 11 GU면 밀어서 만나게 할 확률이 600초에 0%였다. 다만 3.0은 태양보다 공이
  // 커 보여서 2.2로 내리고, 대신 태양 반경을 72→105로 키워 비율을 잡았다.
  // (공 지름이 태양 지름의 절반 이하가 되게 — VIS.MIN_PLANET_PX도 같이 내렸다.)
  // 물리(중력·힐 반경·스윙바이 존)는 여전히 b.radius를 쓰므로 세계는 그대로다.
  // 2.2 → 3.4. 이 값은 예전에도 같은 이유로 1.6→2.2로 올렸던 노브다("궤도 간격이
  // 240 GU인데 공 반경이 11 GU면 밀어서 만나게 할 확률이 600초에 0%였다").
  // 판이 실제 태양계로 바뀌면서 반경이 2000 GU로 넓어지고 궤도 간격도 60~400 GU가
  // 되자 같은 문제가 더 크게 돌아왔다 — 플레이테스트에서 요새를 밀어도 150초 안에
  // 아무것과도 안 부딪혔고, 요새에 닿는 각도조차 720개 중 0~8개였다.
  // 판정 원을 키우면 ① 미사일이 표적을 맞히기 쉬워지고 ② 밀어서 처박기 쉬워진다.
  HIT_R: 3.4,
  A_MIN: 260,
  EARTH_MU: 300, EARTH_R: 11,

  // ─── 행성 인구 ────────────────────────────────────────────────
  // 첫 판은 실제 태양계 15기로 고정이다(stage.js SOLAR). 이후로는 그 성계가
  // 그대로 이어지고 조르그 증원만 얹히므로, 무작위 생성용 상수는 더 없다.
  SUN_BONUS_R: 450,                   // 태양 가속 보너스(§9.1) — R★의 3배 유지
  // 탄약 제한은 없앴다. 대신 **한 번에 한 발**만 날 수 있다 —
  // 쏜 탄이 결판날 때까지 다음 탄을 못 쏘므로, 아끼는 게 아니라 기다리는 게 비용이다.
  // (작전 시한이 진짜 자원이다: 미사일이 나는 동안 시계가 흐른다.)
  // 작전 시한 — 인게임 시간 기준(조준 중엔 안 흐르고, 관측 4×/미사일 비행 중에만 흐른다).
  // 당구는 큐를 놓은 뒤가 본편이라, 전탄 소진 후에는 1×로 자동으로 흐른다.
  // 판이 넓어지고(비행 시간↑) 체력이 생긴 만큼 시한도 함께 늘렸다.
  // 레이저 대응 시간(95초)이 시한 안에서 두어 번은 돌아야 하므로 다시 늘린다.
  TIME_BASE: 640, TIME_PER_ANTE: 40, TIME_BONUS: 0.4,

  // ─── 핵탄두 (당구 큐) ─────────────────────────────────────────
  // 핵은 행성을 부수지 못한다. 오직 "민다". 부수는 건 행성끼리의 충돌뿐이다.
  // 기본 작약을 5→3으로 내렸다. 태양계에는 소행성급(μ 30~90)이 많은데
  // 5Mt이면 Δv가 60~80이라 한 대만 쳐도 벨트까지 날아가 버렸다(플레이테스트).
  // 3Mt이면 소행성 Δv 17~50, 큰 행성 2~5 — "살살 밀기"가 기본값이 된다.
  YIELD_MIN: 1, YIELD_MAX: 12, YIELD_DEFAULT: 3,
  // Δv = NUKE_IMPULSE × 작약량 / μ.  입사각·입사속도와 완전히 무관하다.
  // 방향은 오직 폭심 → 행성 중심, 즉 "공의 어느 쪽을 쳤는가"만으로 정해진다.
  NUKE_IMPULSE: 520,
  BLAST_R: 26,                        // 폭풍 반경 = BLAST_R × 작약량 (주변 천체 2차 압력)
  BLAST_PUSH: 0.30,                   // 2차 압력 계수 (직격 Δv 대비)
  // 근접 신관 반경. 발사는 연속으로 가능하고, 두 탄이 이 반경 안에서 만나면
  // 그 자리에서 동시 기폭한다 — 작약량이 합쳐진다.
  // 20(접촉 40 GU)에서는 요격 창이 각도 1° 미만이라 사실상 못 맞혔다(계측).
  // 신관을 키워 "노려서 되는" 수준으로 올린다.
  MISSILE_HIT_R: 45, INTERCEPT_SCORE: 40,

  // ─── 특수 천체 (roles.js) ────────────────────────────────────
  ARMOR_DV: 0.15,                     // 장갑 — 핵 임펄스 감쇠율
  BATTERY_AMMO: 2,                    // 요새 — 반격 미사일 재고
  BATTERY_SPEED: 30, BATTERY_YIELD: 6,
  COUNTER_ESC_MARGIN: 1.25,           // 반격탄 속도 하한 = 요새 탈출속도 × 이 배수
  VOID_GROW: 0.5, VOID_MU_MAX: 4200,  // 특이점 — 삼킨 질량의 절반을 흡수하며 자란다
  VOLATILE_R: 13, VOLATILE_MIN_R: 240,   // 휘발성 유폭 반경 = 반지름 × 13
  VOLATILE_IMPULSE: 7200,             // 유폭 임펄스: Δv = 이 값 × 감쇠² / 질량
  ROLE_PER_ANTE: 0.5,                 // 안테당 특수 천체 증가량

  // ─── 지속 성계 · 조르그 증원 ─────────────────────────────────
  // 판이 끝나도 성계는 그대로다. 살아남은 행성은 궤도째로 다음 판에 넘어가고,
  // 조르그는 본성에서 새 행성을 워프로 보내온다.
  WARP_TIME: 1.5,                     // 워프인 연출 길이(실시간 초)
  // 판당 새 요새 수 = BASE + PER_ANTE×안테. 요새 하나를 부수는 데 이제 충돌
  // 세 번이 필요하므로(체력 3) 마릿수는 줄인다 — 안테 1에 1기, 안테 8에 4기.
  REINF_BASE: 1, REINF_PER_ANTE: 0.34,
  // 요새는 "밀어서 닿을 수 있는 공" 옆에만 워프한다 — 가장 가까운 이웃 궤도와의
  // 반경 차가 이 값 이내여야 한다. 빈 궤도에 떨어뜨렸더니 1스테이지에서
  // 요새에 피해를 주는 수가 810수 중 0개였다(플레이테스트).
  FORT_NEAR_BAND: 150,
  FORT_MU_MIN: 110, FORT_MU_SPAN: 90,      // 요새 질량 — 밀리는 공이어야 표적이 된다
  FORT_BAND_LO: 0.55, FORT_BAND_HI: 1.6,   // 요새 궤도대 = 지구 궤도 × [lo, hi]
  FORT_CUE_MIN_DV: 12,                // 그 이웃이 최대 작약으로 이만큼은 밀려야 '칠 수 있는 공'이다
  REINF_NEUTRAL: 2,                   // 같이 딸려오는 비위협 조르그 행성 수(상한)
  SYSTEM_CAP: 26,                     // 성계 최대 천체 수 — 넘으면 증원을 줄인다

  // ─── 조르그 레이저 ───────────────────────────────────────────
  // 본성이 지구의 "미래 위치"를 조준해 충전한 뒤 직선으로 쏜다.
  // 충전 시작 시점에 조준선이 고정되므로 ① 다른 행성으로 막거나
  // ② 지구 궤도를 (핵 폭풍으로) 밀어 빗나가게 하는 두 가지 대응이 성립한다.
  //
  // 대응 시간(LASER_CHARGE)이 9초였을 때는 사실상 대응이 불가능했다:
  // 미사일이 목표까지 가는 데만 10~25초가 걸리므로 "경보를 보고 나서 쏘는"
  // 대응이 원리적으로 성립하지 않았다. 충전 시간을 한 발 왕복보다 길게 잡아
  // 경보 → 조준 → 발사 → 착탄 → 궤도 변화가 전부 들어가게 만든다.
  // 충전 시간 = 순수한 대응 시간이다. 45초로도 "경보 → 조준 → 발사 → 착탄 →
  // 궤도 변화"가 빠듯했다(미사일 비행만 10~25초, 밀린 행성이 조준선까지 가는
  // 데 다시 그만큼). 두 발을 쓸 여유가 생기도록 두 배로 늘린다.
  LASER_FIRST: 210,                   // 첫 조준까지 인게임 초
  LASER_PERIOD: 300, LASER_JITTER: 45, // 이후 주기 ± 흔들림
  LASER_CHARGE: 95,                   // 충전(조준선 노출) 시간 = 대응 시간
  LASER_FLASH: 0.7,                   // 발사 섬광 지속
  LASER_PUSH: 5200,                   // 차단한 행성이 받는 임펄스 계수 (Δv = 계수/질량)
  LASER_RANGE_MUL: 3.2,               // 사거리 = aMax × 이 값

  // ─── 카이퍼 벨트 (성계 경계) ─────────────────────────────────
  // 추방은 없앴다. 성계는 이제 **닫힌 당구대**다. 둘레를 도는 얼음 벨트가
  // 쿠션 역할을 하고, 박은 천체는 폭발과 함께 속력(=운동량 크기)을 그대로
  // 유지한 채 반사각으로 되돌아온다. 잃는 건 아무것도 없다 —
  // 밖으로 세게 친 공은 반드시 판 안으로 되돌아와 다시 판돈이 된다.
  BELT_MUL: 1.42,                     // 벨트 반경 = aMax × 이 값
  BELT_THICK: 60,                     // 벨트 띠 두께(연출·판정 여유)
  BELT_RESTITUTION: 1.0,              // 반발계수 1 = 속력 완전 보존 (당구 쿠션)
  BELT_COOLDOWN: 0.8,                 // 같은 천체의 연속 재판정 방지(초)

  // ─── 행성 체력 ───────────────────────────────────────────────
  // 한 번 스치면 즉사하던 규칙을 없앤다. 행성끼리 부딪히면 당구공처럼
  // 운동량을 주고받으며 튕기고, 체력이 1씩 깎인다. 3번 박아야 박살난다.
  // → "한 방에 맞히기"가 아니라 "같은 공을 세 번 몰아붙이기"가 게임이 된다.
  // 조르그 요새는 체력 1 — 제대로 한 번 처박으면 끝난다.
  // 표적을 세 번 맞히는 건 반복이지 난이도가 아니었다(플레이테스트).
  // 중립 행성은 그대로 3이라 큐볼은 여러 번 굴려 쓸 수 있다.
  PLANET_HP: 3, ZORG_HP: 1, EARTH_HP: 3,
  BOUNCE_RESTITUTION: 1.0,            // 행성끼리의 반발계수 — 1 = 완전탄성(운동량·에너지 보존)
  HIT_COOLDOWN: 0.7,                  // 같은 쌍의 재판정 금지 시간(인게임 초)
  COLLIDE_DMG_V: 6,                   // 이 상대속도 미만의 접촉은 데미지 없이 튕기기만
  // 가스 행성이 충돌로 유폭하는 문턱. 이웃 궤도끼리 스치는 자연 접촉(<10)은
  // 넘지 못하고, 핵으로 민 공(Δv = 520×작약/질량, 5Mt·μ200 이면 13)은 넘는다.
  VOLATILE_TRIGGER_V: 18,
}
export const radiusOf = (mu) => 1.6 * Math.cbrt(mu)   // T1-9
// 미사일 명중 반경 — 물리(행성끼리 충돌)는 여전히 b.radius를 쓴다.
// 이 함수는 "미사일이 무엇에 맞는가"에만 쓰이고, 렌더도 같은 값을 그린다.
export const hitRadiusOf = (b) => b.type === 'debris' ? b.radius : b.radius * CFG.HIT_R
// 행성끼리는 "보이는 공"이 닿는 순간 터진다 — 판정과 그림이 같은 원이라
// "닿았는데 안 터졌다"가 원천적으로 없다.
export const contactDist = (a, b) => hitRadiusOf(a) + hitRadiusOf(b)
// 성계 반경 — 공을 열댓 개 깔려면 판이 그만큼 넓어야 한다. 다만 무한정
// 넓힐 수는 없다: 미사일이 TTL 안에 반대편까지 닿아야 판이 성립한다
// (LAUNCH_MAX × MISSILE_TTL ≈ 2900 GU 가 실질 사거리 상한).
export const aMaxOf = (A) => 1700 + 130 * Math.min(A, 6)   // §7.2
// 카이퍼 벨트 반경 — 성계의 쿠션. 최외곽 궤도(0.95 aMax)의 원점 거리보다
// 넉넉히 밖에 둬서 평시 공전이 벨트를 긁지 않게 한다.
export const beltRadius = (aMax) => aMax * CFG.BELT_MUL
// 미사일이 느끼는 탈출속도 — 미사일 중력장은 행성 μ를 κ배로 증폭해서 받는다
// (physics.fieldAccel). 그래서 "이 속도로 쏘면 저 천체를 벗어나는가"는
// 진짜 물리가 아니라 이 식으로 물어야 한다. 반격탄 발사 속도의 근거.
export const escapeSpeed = (mu, d) => Math.sqrt(2 * CFG.KAPPA * mu / Math.max(1, d))
// 반격탄 발사 속도 — 고정 30이던 값은 **요새를 벗어나지도 못하는 속도**였다.
// (μ=300 요새의 발사점 탈출속도는 51.6인데 30으로 쏘고 있었다 → 반격탄이
//  포물선을 그리며 제 요새로 되떨어지거나 그 자리를 맴돌았다.)
// 이제 매번 그 요새의 탈출속도를 계산해 여유배수를 곱한 값과 비교한다.
export const counterSpeed = (b, offset) =>
  Math.max(CFG.BATTERY_SPEED, escapeSpeed(b.mu, offset) * CFG.COUNTER_ESC_MARGIN)
// 핵 임펄스 — 작약량과 표적 질량만의 함수. 이 한 줄이 게임의 조준 규칙 전부다.
export const nukeDv = (yld, mu) => CFG.NUKE_IMPULSE * yld / mu
export const blastRadius = (yld) => CFG.BLAST_R * yld

// ─── 렌더 전용 상수 ───────────────────────────────────────────
// 물리 반지름 R_p = 1.6·μ^(1/3) 은 기획서 §4.1 고정값이라 건드리지 않는다.
// "행성이 커 보이게" 하는 건 전부 여기: 중력권 헤일로(§5.2 스윙바이 존 6R_p,
// SOI 확대율 κ^(2/5)≈5.1× 가 근거) + 화면 최소 크기 바닥값 + 3D 구체 셰이딩.
export const VIS = {
  FOV: 42,                    // 탑다운 원근 카메라 — 시점은 위에서, 입체감은 유지 (§14.1)
  MIN_PLANET_PX: 18,          // 행성 최소 화면 지름(px). 판정 반경은 그대로, 보이기만 키운다
  MIN_DEBRIS_PX: 7,
  // 공을 작게 만든 대신 부풀림 상한을 올렸다. 전체 뷰에서 20개가 점으로
  // 사라지면 "많다"가 정보가 아니라 노이즈가 된다.
  MAX_INFLATE: 2.2,           // 최소 크기 바닥값이 실제 반경을 넘길 수 있는 최대 배율
  TRUE_R_HINT: 1.05,          // 최소크기 바닥값으로 부풀었을 때만 진짜 공 반경을 얇은 링으로 병기
  ICON_PX: 22,                // 행성 카테고리 아이콘 화면 크기(px)
  ZOOM_MIN: 0.42, ZOOM_MAX: 14, ZOOM_STEP: 1.4,
  FIT_MARGIN: 1.25,           // 자동 프레이밍 여백
  FULL_FIT: 1.58,             // §14.1 성계 전체 뷰 — 카이퍼 벨트(1.42 aMax)까지 들어가게
  SHAKE_MAX: 46,              // 명중 스크린셰이크 최대 진폭(GU) — 핵은 과장한다
  PARTICLES: 16000,
}
