// 기획서 부록 A — 밸런스 상수표. 모든 숫자는 이 파일에서만 튜닝한다.
export const CFG = {
  // 태양 반경 105 → 150. 판정 원(HIT_R)을 3.4로 키우면서 목성의 그려지는 원이
  // 111 GU가 되어 태양(105)보다 커 보였다 — 태양이 판의 중심으로 안 읽힌다.
  // 덤으로 "태양 포켓"이 넓어져 떨어뜨려 처분하는 선도 살아난다.
  DT: 1 / 120, EPS: 3, MU_STAR: 3e5, R_STAR: 150,
  // κ = 미사일이 받는 행성 중력 배율(§4.3). κ★ = 태양 배율.
  // 기획서 원본은 κ=60 / 태양 1.0이었으나 계측 결과 인카운터 평균 최대굴절이
  // 2.2°(문턱 25°)에 그쳐 스윙바이가 사실상 발생하지 않았다(§18 R1).
  // κ↑ + 태양↓ 로 평균굴절 55°까지 올린다. 행성끼리는 여전히 진짜 물리(κ=1).
  KAPPA: 120, KAPPA_STAR: 0.6,
  SUBSTEP_DIST: 5, SUBSTEP_N: 4,
  // 발사 속도 P_v (§5.1). 하한은 **지구의 κ증폭 탈출속도 위**여야 한다.
  // (구값 18은 KAPPA=60 시절의 계산이라 KAPPA=120인 지금은 지구 탈출 22.7보다
  //  낮았다 — 쏜 탄이 지구로 되떨어지는 자살 대역이 열려 있었다.)
  // 상한은 카이퍼 벨트가 튕겨 주므로 예전만큼 조심할 필요가 없다.
  LAUNCH_MIN: 26, LAUNCH_MAX: 56,
  // 발사 오프셋 — 지구 중력권 밖에서 쏜다. 구값(R_E+8=19)에서는 지구의
  // κ증폭 탈출속도가 43.5로 태양 탈출속도(36.5)보다 높아서
  // "지구를 벗어나면 반드시 성계도 벗어나는" 상태였다.
  // ※ 여기서 지구의 κ증폭 탈출속도는 escapeSpeed(EARTH_MU, LAUNCH_OFFSET) 로
  //   계산된다(아래). LAUNCH_MIN은 그 값 위에 있어야 한다 — 안 그러면 쏜 탄이
  //   지구 중력권을 못 벗어나고 되돌아와 자기 지구에 박힌다.
  LAUNCH_OFFSET: 140,
  MISSILE_TTL: 52,                    // 미사일 자폭(초). 성계가 넓어진 만큼 늘렸다
  // 결판난 탄의 궤적이 화면에서 걷히기까지(실시간 초). 예전에는 판이 끝날
  // 때까지 남겼는데, 탄이 무제한이라 한 판에 열댓 줄이 쌓여 회색 그물이 됐다.
  // "방금 그 탄이 어디로 갔나"를 확인할 만큼만 남기고 지운다.
  SHOT_TRAIL_FADE: 3.5,
  SWING_ZONE: 6, SWING_DEG: 25, NEAR_MISS: 2.8, CAPTURE_WRAP: 0.75,   // §5.2
  // 당구공 반경 배율. 이 게임에는 이제 **원이 하나뿐이다**:
  // 그리는 원 = 미사일이 맞는 원 = 행성끼리 부딪히는 원.
  // 1.6이던 값을 올린 건 계측 결과다(§18 R2): 궤도 간격이 240 GU인데 공 반경이
  // 11 GU면 밀어서 만나게 할 확률이 600초에 0%였다. 다만 3.0은 태양보다 공이
  // 커 보여서 2.2로 내리고, 대신 태양 반경을 72→105로 키워 비율을 잡았다.
  // (공 지름이 태양 지름의 절반 이하가 되게 — VIS.MIN_PLANET_PX도 같이 내렸다.)
  // 물리(중력·힐 반경·스윙바이 존)는 여전히 b.radius를 쓰므로 세계는 그대로다.
  // 2.2 → 3.4. 이 값은 예전에도 같은 이유로 1.6→2.2로 올렸던 노브다("궤도 간격이
  // 240 GU인데 공 반경이 11 GU면 밀어서 만나게 할 확률이 600초에 0%였다").
  // 판이 실제 태양계로 바뀌면서 반경이 2000 GU로 넓어지고 궤도 간격도 60~400 GU가
  // 되자 같은 문제가 더 크게 돌아왔다 — 플레이테스트에서 요새를 밀어도 150초 안에
  // 아무것과도 안 부딪혔고, 요새에 닿는 각도조차 720개 중 0~8개였다.
  // 판정 원을 키우면 ① 미사일이 표적을 맞히기 쉬워지고 ② 밀어서 처박기 쉬워진다.
  HIT_R: 3.4,
  // 판정 원의 **하한**(GU). 작은 소행성은 반경 × HIT_R 이 8~9밖에 안 돼서
  // 화면에서 점이 된다. 예전에는 그걸 "그림만 부풀려" 가렸는데, 그러면 보이는
  // 원과 판정 원이 어긋나 겹쳤는데 통과하는 사고가 났다.
  // 이제는 반대로 간다: **판정을 올려서** 그림을 예전 크기로 되돌린다.
  // 18은 예전에 소행성이 가장 크게 그려지던 값(성계 전체 보기)이다 —
  // 어느 공도 예전보다 작아 보이지 않는다. 그리고 카메라와 무관한 값이라
  // 줌을 바꿔도 판정이 흔들리지 않는다.
  MIN_HIT_R: 18,
  A_MIN: 260,
  EARTH_MU: 300, EARTH_R: 11,

  // ─── 행성 인구 ────────────────────────────────────────────────
  // 첫 판은 실제 태양계 15기로 고정이다(stage.js SOLAR). 이후로는 그 성계가
  // 그대로 이어지고 조르그 증원만 얹히므로, 무작위 생성용 상수는 더 없다.
  // 탄약 제한은 없앴다. 대신 **한 번에 한 발**만 날 수 있다 —
  // 쏜 탄이 결판날 때까지 다음 탄을 못 쏘므로, 아끼는 게 아니라 기다리는 게 비용이다.
  // (작전 시한이 진짜 자원이다: 미사일이 나는 동안 시계가 흐른다.)
  // 작전 시한 — 인게임 시간 기준. 조준 모드에서는 멈춰 있고(시간 진행 버튼을
  // 누르는 동안만 1~2×), 관측 모드에서 플레이어가 고른 배속(1·2·4·8×)으로 흐른다.
  // 판이 넓어지고(비행 시간↑) 체력이 생긴 만큼 시한도 함께 늘렸다.
  // 레이저 대응 시간(95초)이 시한 안에서 두어 번은 돌아야 하므로 다시 늘린다.
  // 시한을 남겨도 보너스는 없다 — 시한은 보상이 아니라 벽이다(넘기면 모성이 온다).
  TIME_BASE: 640, TIME_PER_ANTE: 40,

  // ─── 정치자금 ────────────────────────────────────────────────
  // 이 게임의 유일한 화폐다. 예전에는 120점·60점·8×배율 같은 수십~수백 단위
  // 점수가 쌓였는데, 그 숫자는 **아무 판단도 바꾸지 않았다** — 쓸 데가 없었기
  // 때문이다. 그래서 전부 걷어내고 한 자릿수 화폐 하나만 남긴다.
  //
  // 버는 법은 셋뿐이고, 셋 다 "잘 쳤다"의 정의 그대로다:
  //   · 스윙바이 1 — 중력으로 휘어 친 값. 이 게임이 가르치려는 기술 그 자체다.
  //   · 일반 조르그 행성 격파 2 — 판돈을 회수한 값. 체력 1이라 한 방이면 끝난다.
  //   · 엘리트 이상 격파 3 — 대형 요새(체력 2)와 모함(체력 5). 한 방으로 안
  //     끝나는 것들이라 값이 한 칸 위다. 판정은 **체력 상한 하나**로 한다
  //     (hpMax > 1) — 등급표를 따로 들고 다니면 새 조르그가 생길 때마다
  //     두 곳을 고쳐야 하고, 그 둘이 어긋나는 순간 값이 조용히 틀어진다.
  // 상품이 5원이므로 **한 판에 하나 살까 말까**가 기본 박자다: 스윙바이 세 번을
  // 엮은 한 발이 곧 물건 하나다.
  POL_SWING: 1, POL_FORT: 2, POL_FORT_ELITE: 3, SHOP_COST: 5,
  // 상한 성장 — 스테퍼의 '보통' 칸(속도 5 · 작약 3)과 같은 폭이다. 한 번 사면
  // 스테퍼를 한 칸 더 밀 수 있다는 뜻이라, 구매가 곧 조작으로 번역된다.
  BUY_SPEED_STEP: 5, BUY_YIELD_STEP: 3, BUY_MAX: 3,
  // 지구의 소모형 추진기 — 조르그가 겨누고 있을 때 한 번 태워 레이저를 피한다.
  // 요새가 미사일을 피하는 회피 분사(FORT_DODGE_*)와 같은 물건이다: 한 방의
  // 임펄스, 진입 방향에 직각, 태양 반대쪽. 태양 탈출 점화처럼 계속 태우지 않는다.
  //
  // Δv 2.5의 근거: 레이저 회랑의 폭은 지구 판정 지름(≈74 GU)이다. 충전이 수십
  // 초 남은 시점이면 이 Δv로 회랑을 벗어나고도 남는다. 궤도 속도(≈25 GU/s)의
  // 10%라 궤도가 망가지지도 않는다 — 피하는 데 필요한 만큼만이다.
  //
  // **판을 안 가린다.** 예전에는 3스테이지부터 열렸는데(요새가 추진기를 달고
  // 오는 판의 다음 판), 그러면 1~2스테이지의 첫 광선은 대응 수가 셋뿐이다
  // — 차폐·궤도 밀기·선제 격파. 지금은 한 발을 손에 쥔 채로 시작하므로
  // 첫 경보에서 바로 넷째 수를 배운다. 그 한 발이 곧 **횟수가 자원**이라는
  // 교습이기도 하다: 다시 채우려면 정치자금 5를 내야 한다(shop.js).
  THRUST_MAX: 3, THRUST_START: 1, EARTH_BURN_DV: 2.5,

  // ─── 핵탄두 (당구 큐) ─────────────────────────────────────────
  // 핵은 행성을 부수지 못한다. 오직 "민다". 부수는 건 행성끼리의 충돌뿐이다.
  // ※ 전역 질량 배율(MU_SCALE)로 판 전체를 가볍게 만들었다. 당구는 공이 굴러야
  //   당구다 — 한 방에 궤도가 눈에 띄게 바뀌어야 "쳤다"는 감각이 생긴다.
  //   반경(=보이는 크기·판정 원)은 질량과 분리돼 있으므로 그림은 그대로다.
  // 기본 작약을 5→3으로 내렸다. 태양계에는 소행성급(μ 30~90)이 많은데
  // 5Mt이면 Δv가 60~80이라 한 대만 쳐도 벨트까지 날아가 버렸다(플레이테스트).
  // 3Mt이면 소행성 Δv 17~50, 큰 행성 2~5 — "살살 밀기"가 기본값이 된다.
  YIELD_MIN: 1, YIELD_MAX: 12, YIELD_DEFAULT: 3,
  // Δv = NUKE_IMPULSE × 작약량 / μ.  입사각·입사속도와 완전히 무관하다.
  // 방향은 오직 폭심 → 행성 중심, 즉 "공의 어느 쪽을 쳤는가"만으로 정해진다.
  NUKE_IMPULSE: 520,
  MU_SCALE: 0.45,                     // 전역 질량 배율 — 낮을수록 핵이 잘 먹힌다
  BLAST_R: 26,                        // 폭풍 반경 = BLAST_R × 작약량 (주변 천체 2차 압력)
  BLAST_PUSH: 0.30,                   // 2차 압력 계수 (직격 Δv 대비)

  // ─── 특수 천체 (roles.js) ────────────────────────────────────
  // 금속 행성 감쇠 — 0.15는 사실상 "안 움직임"이라 태그가 곧 사망 선고였다.
  // 0.5면 확실히 무겁되(다른 공의 절반) 핵으로도 밀 수는 있다 — 선택지가 남는다.
  ARMOR_DV: 0.5,
  VOID_GROW: 0.5, VOID_MU_MAX: 4200,  // 특이점 — 삼킨 질량의 절반을 흡수하며 자란다
  // 가스 유폭 — 반경과 힘을 함께 키웠다. 가스 행성 하나가 터지면
  // 그 구역의 판이 통째로 재배치되어야 "터진다"는 태그값을 한다.
  VOLATILE_R: 22, VOLATILE_MIN_R: 420,   // 유폭 반경 = 반지름 × 22 (최소 420)
  VOLATILE_IMPULSE: 16000,            // 유폭 임펄스: Δv = 이 값 × 감쇠² / 질량

  // ─── 지속 성계 · 조르그 증원 ─────────────────────────────────
  // 판이 끝나도 성계는 그대로다. 살아남은 행성은 궤도째로 다음 판에 넘어가고,
  // 조르그는 본성에서 새 행성을 워프로 보내온다.
  WARP_TIME: 1.5,                     // 워프인 연출 길이(실시간 초)
  WARP_LEAD: 0.5, WARP_STAGGER: 0.9,  // 첫 워프까지 / 그다음부터 한 기씩 도착 간격
  // 도착이 전부 끝나기까지의 최대 폭(초). 막은 **마지막 한 기가 다 나타날
  // 때까지** 내려가 있으므로, 마릿수가 늘어도 개막이 무한정 길어지면 안 된다.
  // 그래서 간격 쪽을 조인다 — 세 기면 0.9초 그대로, 열 기면 0.5초로 붙는다.
  WARP_SPAN: 4.5,
  // 스폰이 **전부** 끝나고 UI가 떠오르기까지의 뜸(초, 워프인과 같은 클럭).
  // 마지막 요새가 자리를 잡은 화면을 한 박자 보여 준 뒤에 조작을 넘긴다 —
  // 판을 다 보기도 전에 패널이 들이밀면 무엇이 왔는지를 못 읽는다.
  WARP_SETTLE: 1.2,
  // 판당 새 요새 수 = round(BASE + PER_ANTE×안테).
  // 요새는 체력 1이라 한 기당 한 방이면 끝나므로 마릿수로 난이도를 준다 —
  // 안테가 오를수록 조르그가 더 몰려온다. 안테 1에 2기 → 안테 8에 8기.
  REINF_BASE: 1.2, REINF_PER_ANTE: 0.85,
  // 요새는 "밀어서 닿을 수 있는 공" 옆에만 워프한다 — 가장 가까운 이웃 궤도와의
  // 반경 차가 이 값 이내여야 한다. 빈 궤도에 떨어뜨렸더니 1스테이지에서
  // 요새에 피해를 주는 수가 810수 중 0개였다(플레이테스트).
  FORT_NEAR_BAND: 150,
  FORT_MU_MIN: 200, FORT_MU_SPAN: 160,     // 요새 질량(MU_SCALE 적용 전) — 실효 90~160
  // 대형 요새 — 2스테이지부터 섞여 온다. 체력 2라 한 번 처박아서는 안 부서지고,
  // 질량이 두 배라 눈에 띄게 크다(반지름 ∛2 = 1.26배, 판정 원은 그 3.4배).
  // 더 무겁게 잡으면 핵으로는 아예 안 밀려서 "요새는 밀리는 공"이라는 전제가 깨진다.
  FORT_HEAVY_MU: 2.0, FORT_HEAVY_HP: 2,
  // …다만 질량에서 오는 1.26배로는 **체력이 두 배라는 게 안 보였다.** 반지름은
  // 질량의 세제곱근이라 두 배 무거워도 눈에는 4분의 1밖에 안 커진다(계측:
  // 판정 원 25 → 37, 나란히 놓기 전에는 구분이 안 된다). 체력이 곧 덩치라는
  // 규칙을 세우고 반경을 따로 한 번 더 키운다 — 이제 25 대 48, 두 배로 벌어진다.
  // (커진 만큼 맞히기도 쉬워진다. 체력 2를 두 방에 부수라는 뜻이므로 맞다.)
  FORT_HEAVY_R: 1.3,
  FORT_BAND_LO: 0.55, FORT_BAND_HI: 1.6,   // 요새 궤도대 = 지구 궤도 × [lo, hi]
  FORT_CUE_MIN_DV: 12,                // 그 이웃이 최대 작약으로 이만큼은 밀려야 '칠 수 있는 공'이다
  // ─── 요새의 회피 분사 ────────────────────────────────────────
  // 3스테이지부터 요새는 추진기를 하나씩 달고 온다. 제 몸에 꽂힐 궤도로 탄이
  // 들어오면 그 진입 방향에 직각으로, 두 직각 중 태양 반대쪽으로 밀어낸다.
  // 횟수 제한은 없다 — 막는 것은 연료가 아니라 반응 시간이다(아래 REACT).
  //
  // 세기는 제 질량을 따라간다 — **무거운 요새일수록 세게 분사한다.**
  // 예전에는 추력이 질량에 비례해서 Δv는 결국 질량과 무관한 일정 비율(0.34)이
  // 됐다. 그러면 큰 요새나 작은 요새나 똑같이 비켜서고, 화면에서 "저 덩치가
  // 저만큼밖에 못 미나" 하는 위화감만 남는다. 이제 추력은 질량보다 더 가파르게
  // 커져서 Δv 자체가 질량에 비례한다: Δv = 지금 속도 × DV × (질량 / MU_REF).
  // 대신 대형 요새(질량 2배·체력 2)가 그만큼 잘 피한다 — "덩치 = 무적"을 막는
  // 것은 세기가 아니라 반응 시간(REACT)과 질량 배수 상한이다.
  // 세진 만큼 궤도도 크게 틀어지므로 분사한 요새가 벨트까지 나갔다 오는 경우가
  // 늘어난다(계측: 25% → 46%). 그게 세진 추진기의 대가다 — 판이 벨트로 공을
  // 되돌려 보내므로 표적을 잃지는 않는다.
  //
  // 비켜나는 거리는 Δv 하나로 안 정해진다. 이 판의 탄은 행성 중력을 κ=120배로
  // 받으므로(§4.3), 옆으로 비켜서도 요새의 중력 우물이 탄을 도로 끌어당긴다 —
  // 계측에서 60 GU를 비켜서고도 다시 빨려 들어와 맞았다. 그래서 **미리 아는
  // 시간**이 세기만큼 중요하다. 22초·0.34가 열 시드 전부에서 회피에 성공한
  // 가장 약한 조합이다(10초·0.26은 8/10, 18초·0.26은 8/10, 18초·0.34는 9/10).
  FORT_DODGE_STAGE: 2,                // 이 스테이지 인덱스부터(0-기반 → 3스테이지)
  FORT_DODGE_LEAD: 22,                // 이 시간(인게임 초) 안에 꽂힐 탄이면 분사
  FORT_DODGE_DV: 0.34,                // 기준 질량에서의 Δv 비율 (= 지금 속도 × 이 값)
  // 질량 배수 = 실효 질량 / MU_REF, [MIN, MAX]로 자른다.
  // 요새 실효 질량은 90~210(대형은 그 2배, 180~425)이므로 MU_REF를 90으로 잡으면
  // 가장 가벼운 요새가 딱 예전 세기(×1)이고 — **누구도 약해지지 않는다** —
  // 대형 요새는 상한 3.0에 붙는다(계측: 분사 넷 중 셋이 세지고, 중앙값 1.2배 ·
  // 상위 5%가 2.3배다). 상한이 없으면 후반 안테의 무거운 요새가 저 혼자 궤도를
  // 통째로 갈아 치운다 — 회피가 아니라 이사다.
  FORT_DODGE_MU_REF: 90,
  FORT_DODGE_MASS_MIN: 1, FORT_DODGE_MASS_MAX: 3,
  // 하나만은 세기와 무관하게 막는다: **스스로 태양에 처박는 분사.**
  // 미는 방향은 탄에 직각이라 궤도 진행 방향과 반대로 걸릴 때가 있는데, 그때
  // Δv가 커지면 궤도 속력을 깎아 근일점이 태양까지 내려간다 — 회피하려다 자살한
  // 요새는 플레이어가 이해할 수 없는 그림이다. 그래서 분사한 궤도의 근일점이
  // 이 반경(태양 반경 150의 1.6배 — 코로나 밖) 안쪽이면 세기를 한 단계씩 낮춘다.
  // 속력이 아니라 반경으로 재야 맞다 — 속력만 보면 바깥으로 크게 민 궤도의
  // 근일점이 같이 내려가는 걸 놓친다.
  // 단, 어떤 안전장치도 예전 세기(FORT_DODGE_DV) 아래로는 깎지 않는다. 예전에도
  // 역행으로 밀리면 태양에 처박는 궤도가 나왔고(계측 4.8%), 그건 이 변경이
  // 만든 게 아니라 원래 있던 성질이다. 여기서 같이 고치면 "세게"가 아니라
  // "다르게"가 된다.
  FORT_DODGE_PERI_MIN: 240,
  FORT_DODGE_TICK: 0.3,               // 위협 판정 주기(인게임 초) — 매 스텝은 비싸다
  // ── 반응 시간 ──
  // 위협을 본 즉시 분사하면 요새는 **무조건 피하는 물건**이 된다. 그러면 첫 발은
  // 언제나 버리는 발이고, 플레이어가 고를 수 있는 건 아무것도 없다.
  // 그래서 눈치채고 나서 실제로 태우기까지 시간이 걸린다 — 그동안 탄이 이미
  // 닿아 버리면 못 피한다. 즉 **늦게 알아챈 궤도**(스윙바이로 막판에 꺾여 들어온
  // 탄, 가까이서 쏜 탄)는 그대로 꽂힌다.
  // 요새마다 조금씩 다르다(JITTER) — 여덟 기가 같은 초에 일제히 태우면 그건
  // 반응이 아니라 스크립트로 보인다.
  //
  // 값은 계측으로 잡았다. 18시드에서 요새에 정통으로 꽂히는 한 발을 풀어 두고
  // 반응 시간만 바꿔 가며 다시 굴린 결과(회피율 = 그 발이 빗나간 비율):
  //   0s 89% · 4s 89% · 7±4s 89% · 10s 78% · 12±5s 67% · 16s 67% · 22s 0%
  // 7초까지는 즉발과 구분이 안 된다(앞서 보는 시간이 22초라 여유가 남는다).
  // 22초는 반응이 곧 사거리라 한 번도 못 피한다.
  //
  // 값은 12±5초에서 **6±2.5초로 절반**이다. 분사가 세진 만큼 늦게 눈치채는
  // 맛으로 버티게 두면 요새가 두 번 물러지므로, 반응은 도로 빠르게 당긴다.
  // 6초는 위 계측에서 4초와 7초 사이 — 즉발과 거의 구분이 안 되는 구간이라
  // "가까이서 쏜 탄·막판에 꺾인 탄만 꽂힌다"는 성질만 남는다.
  FORT_DODGE_REACT: 6,                // 위협 포착 → 분사까지(인게임 초)
  FORT_DODGE_REACT_JITTER: 2.5,       // ± 이만큼 요새마다 흔들린다
  REINF_NEUTRAL: 2,                   // 같이 딸려오는 비위협 조르그 행성 수(상한)
  SYSTEM_CAP: 34,                     // 성계 최대 천체 수 — 넘으면 증원을 줄인다

  // ─── 조르그 모함 (안테 5부터) ────────────────────────────────
  // 요새가 늘어나는 것만으로는 후반 판이 "같은 일을 여덟 번"이 된다. 그래서
  // 안테 5부터 **판을 굴리는 물건**을 하나 넣는다: 궤도 바깥에 앉은 거대
  // 조르그 행성. 이놈은 광선을 안 쏜다 — 대신 **부술 때까지 성계를 채운다.**
  // 요새가 줄면 요새를 워프시키고, 굴릴 공이 없으면 중립 행성을 워프시킨다.
  //
  // 그래서 후반의 판단이 하나 바뀐다: 눈앞의 요새를 먼저 칠 것인가, 멀리 있는
  // 모함부터 끊을 것인가. 요새만 부수면 그 자리가 계속 다시 차오르고,
  // 모함을 부수면 그 순간부터 판은 유한해진다.
  //
  // 모성(DOOM_*)과는 다르다: 모성은 시한 종료 통보라 부술 수 없고, 이쪽은
  // **부수라고 있는 물건**이다. 체력 5 — 이 판에서 제일 단단하다.
  HIVE_ANTE: 5,                       // 이 안테부터 한 기 (없으면 다음 판에 또 온다)
  HIVE_HP: 5,
  HIVE_MU: 1000,                      // 무겁다 — 핵으로는 안 밀린다(Δv 6). 공을 던져야 한다
  HIVE_R: 26,                         // 크다(판정 원 88 GU) — 멀어도 맞힐 수는 있다
  // 지구 궤도 × [lo, hi] — 요새 대역(0.55~1.6) **바로 바깥**.
  // 2.35까지 밀었더니 밀어 보낸 공이 모함까지 가는 데만 200초가 넘었다
  // (계측: 자동 플레이가 110초 지평 안에서 모함에 닿는 수를 한 번도 못 찾았다).
  // 멀리 있는 건 그대로 두되, "한 번 더 굴리면 닿는" 거리로 당긴다.
  HIVE_BAND: [1.65, 2.15],
  // 보충 간격(인게임 초). 한 번에 한 기씩만.
  // 55초로 잡았더니 요새를 부수는 속도(대개 60~120초에 한 기)를 그대로 따라잡아
  // "모함부터"가 아니라 "모함 말고는 아무것도 못 한다"가 됐다. 75초면 잘 치는
  // 판은 요새를 조금씩 줄여 나갈 수 있고, 그래도 결국 모함을 끊어야 끝난다.
  HIVE_PERIOD: 90,
  HIVE_FIRST: 40,                     // 도착 후 첫 보충까지 — 판이 열리자마자 밀지 않는다
  HIVE_KEEP_CUE: 6,                   // 굴릴 중립 천체가 이보다 적으면 하나 보낸다
  // 모함이 되채우는 **수준**. 판이 열릴 때 온 요새 수의 이 비율까지만 채운다.
  // 1.0(전부 되채움)으로 뒀더니 요새를 부수는 족족 그 자리가 메워져서, 모함에
  // 닿는 긴 한 수를 준비하는 동안 판이 한 발짝도 안 줄었다 — 그건 판단이 아니라
  // 러닝머신이다. 0.6이면 부순 만큼은 남고, 그래도 바닥은 모함이 받쳐 준다.
  HIVE_KEEP_FRAC: 0.6,
  // 모함도 **밀어서 닿을 수 있는 공** 옆에 앉아야 한다. 핵은 모함을 못 부수므로
  // (체력 5 · μ1000 이라 Δv 6) 공을 던지는 것 말고는 방법이 없는데, 이웃이 전부
  // 목성·토성 같은 덩치면 던질 공이 없다. 요새 기준(150)보다 넉넉히 잡는 이유는
  // 바깥 궤도가 성기기 때문이다.
  HIVE_NEAR_BAND: 300,
  // 모함이 있는 판에는 시한을 이만큼 더 준다. 모함은 **판 바깥에 있는 두 번째
  // 목표**라, 요새 다섯을 치던 시간에 그것까지 하라는 건 시간이 아니라 운을
  // 요구하는 것이다(계측: 800초 판에서 자동 플레이가 3시드 중 1건만 클리어).
  HIVE_TIME: 150,

  // ─── 조르그 레이저 ───────────────────────────────────────────
  // **요새는 전부 쏜다.** 예전에는 그중 하나(본성)만 포대였고 나머지는 그냥
  // 서 있는 표적이었는데, 그러면 "저 요새들은 대체 왜 위협인가"에 그림이
  // 답하지 못했다. 이제 요새 하나하나가 지구를 겨눈다 — 다만 **동시에**는
  // 아니다. 주기는 모두 같고 시작 시각만 어긋나 있어서, 한 기가 쏘고 나면
  // 다음 기가 물고, 그다음이 물린다. 요새를 하나 부술 때마다 그 순번이
  // 통째로 사라지므로, 격파가 곧 숨통이다.
  //
  // 요새가 지구의 "미래 위치"를 조준해 충전한 뒤 직선으로 쏜다.
  // 충전 시작 시점에 조준선이 고정되므로 ① 다른 행성으로 막거나
  // ② 지구 궤도를 (핵 폭풍으로) 밀어 빗나가게 하는 두 가지 대응이 성립한다.
  //
  // 대응 시간(LASER_CHARGE)이 9초였을 때는 사실상 대응이 불가능했다:
  // 미사일이 목표까지 가는 데만 10~25초가 걸리므로 "경보를 보고 나서 쏘는"
  // 대응이 원리적으로 성립하지 않았다. 충전 시간을 한 발 왕복보다 길게 잡아
  // 경보 → 조준 → 발사 → 착탄 → 궤도 변화가 전부 들어가게 만든다.
  // 충전 시간 = 순수한 대응 시간이다. 45초로도 "경보 → 조준 → 발사 → 착탄 →
  // 궤도 변화"가 빠듯했다(미사일 비행만 10~25초, 밀린 행성이 조준선까지 가는
  // 데 다시 그만큼). 두 발을 쓸 여유가 생기도록 두 배로 늘린다.
  // 첫 조준(1번 요새)까지 인게임 초. **판이 거듭될수록 당겨진다.**
  // 210초 고정이던 시절에는 첫 광선이 시한의 절반이 지나서야 물렸고, 그동안
  // 조르그는 그냥 서 있는 표적이었다 — 후반 판일수록 "요새를 천천히 하나씩"이
  // 성립해 버려서 광선이 판을 압박하지 못했다.
  // 다만 1스테이지는 규칙을 배우는 판이라 크게 당기지 않는다: 180초(-30).
  // 첫 광선이 실제로 닿는 시각은 여기에 충전 95초를 더한 값이다.
  //   판 1: 180(착탄 275) · 판 2: 156 · 판 3: 132 · 판 4 이후: 108(착탄 203)
  LASER_FIRST: 180,                   // 1스테이지 첫 조준
  LASER_FIRST_DROP: 24,               // 판마다 이만큼 당겨진다
  LASER_FIRST_MIN: 108,               // 하한 — 이보다 이르면 개막부터 경보다
  LASER_PERIOD: 300, LASER_JITTER: 45, // 이후 주기 ± 흔들림 (요새 하나 기준)
  // 요새끼리의 시차(인게임 초). 1번이 물고 이만큼 뒤에 2번, 또 이만큼 뒤에 3번.
  // 그래서 요새가 몇 기든 **성계 전체가 겪는 박자는 이 값 하나**로 정해진다:
  // 실제 주기 = max(LASER_PERIOD, LASER_SPACING × 살아 있는 요새 수).
  // 여덟 기가 저마다 300초 주기로 쏘면 37초에 한 발이라 대응 자체가 성립하지
  // 않는다 — 마릿수는 난이도지 물량이 아니다.
  LASER_SPACING: 150,
  LASER_CHARGE: 95,                   // 충전(조준점 노출) 시간 = 대응 시간
  // 광선은 즉발이 아니다 — 정해진 속도로 날아간다. 지구까지가 대개 1000~1500 GU라
  // 900이면 도달에 1~2초: "빠르지만 날아가는 게 보인다"의 경계값이다.
  // (미사일이 30~56 GU/s이니 대략 20배 빠르다.)
  LASER_SPEED: 900,                   // GU/s — 광선 진행 속도
  // 광선 막대의 길이(GU). 광선은 무한한 선이 아니라 **길이가 있는 덩어리**다.
  // 그래서 머리가 무언가에 박혀 멎어도 꼬리는 그 자리까지 계속 밀려 들어간다 —
  // 닿는 순간 통째로 사라지면 "빛이 사라졌다"가 아니라 "그리기를 멈췄다"로 읽힌다.
  // 머리가 멎은 뒤 다 빨려 들어가는 데 걸리는 시간 = 이 값 / LASER_SPEED (0.18초).
  LASER_BOLT: 160,
  LASER_RANGE_MUL: 3.2,               // 사거리 = aMax × 이 값
  // 광선 요격 — 날아가는 광선의 진로에 내 탄두가 걸려 있으면 그 자리에서
  // 터뜨려 막을 수 있다. 광선이 초당 900 GU로 지나가므로 이 반경이 곧
  // 타이밍 창이다: 60이면 2×60/900 = 0.13초. 조준선이 충전 내내 보이므로
  // "미리 그 선 위에 탄을 걸쳐 둔다"가 성립하는 수다.
  LASER_INTERCEPT_R: 60,
  // ─── 시한 종료: 조르그 모성 ─────────────────────────────────
  // 시한을 넘기면 모성이 워프해 들어와 사방에 광선을 뿌리고 판을 끝낸다.
  // 이길 수 있는 싸움이 아니다 — 문장으로 통보하는 대신 보여 주는 연출이다.
  DOOM_WARP: 1.6,                     // 워프인 후 난사 시작까지(인게임 초)
  // **쉬지 않는다.** 예전에는 18발을 한 차례로 묶고 5초를 쉰 다음 다음 차례를
  // 시작했는데, 그 5초 동안 화면이 조용해져서 "끝났나?" 하는 틈이 생겼다.
  // 이제 이 간격으로 **지구가 부서질 때까지** 끊김 없이 뿜는다. 차례도, 쉼도,
  // 상한도 없다 — 끝을 정하는 것은 시간이 아니라 지구다.
  // (행성이 대신 막아 주면 그만큼 더 산다. 다만 결말은 정해져 있다.)
  DOOM_GAP: 0.22,                     // 광선 사이 간격(인게임 초)
  // 몇 발에 한 번은 지구를 정조준한다. 전부 사방으로만 흩뿌리면 난사는 그림만
  // 남고 아무 일도 안 일어나고, 전부 지구만 겨누면 그건 난사가 아니라 처형이다.
  DOOM_AIM_EVERY: 5,
  // 안전 상한 — 지구가 끝내 안 부서지는 판(행성이 계속 대신 맞는다)에서도
  // 난사가 영원히 이어지지는 않게 한다. 132초어치다. 여기 닿으면 그냥 끝낸다.
  DOOM_MAX_BEAMS: 600,
  DOOM_MU: 1400, DOOM_R: 34,          // 모성 질량 / 반경 — 부술 수 없다

  // ─── 카이퍼 벨트 (성계 경계) ─────────────────────────────────
  // 추방은 없앴다. 성계는 이제 **닫힌 당구대**다. 둘레를 도는 얼음 벨트가
  // 쿠션 역할을 하고, 박은 천체는 폭발과 함께 속력(=운동량 크기)을 그대로
  // 유지한 채 반사각으로 되돌아온다. 잃는 건 아무것도 없다 —
  // 밖으로 세게 친 공은 반드시 판 안으로 되돌아와 다시 판돈이 된다.
  //
  // **단, 미사일은 예외다: 벨트에 닿으면 그 자리에서 터진다.**
  // 공은 쿠션에 튕겨 판돈으로 돌아오지만 탄두는 아니다 — 튕긴 탄은 TTL 52초를
  // 다 쓸 때까지 판 바깥 둘레를 배회하다 아무것도 못 하고 자폭하는 일이 많았고
  // (그동안 다음 탄을 못 쏜다 — inFlight), 그 배회를 예측선으로 읽는 것도
  // 불가능했다. 이제 벨트는 탄에게 **끝나는 자리**다: 판 밖으로 나가는 발은
  // 거기서 결판나고, 차례가 바로 돌아온다. 폭풍은 그대로 터지므로 벨트 근처의
  // 공을 밀 수는 있다 — 다만 그건 노리고 하는 수여야 한다.
  BELT_MUL: 1.42,                     // 벨트 반경 = aMax × 이 값
  BELT_THICK: 60,                     // 벨트 띠 두께(연출·판정 여유)
  BELT_RESTITUTION: 1.0,              // 반발계수 1 = 속력 완전 보존 (당구 쿠션)
  BELT_COOLDOWN: 0.8,                 // 같은 천체의 연속 재판정 방지(초)

  // ─── 행성 체력 ───────────────────────────────────────────────
  // 한 번 스치면 즉사하던 규칙을 없앤다. 행성끼리 부딪히면 당구공처럼
  // 운동량을 주고받으며 튕기고, 체력이 1씩 깎인다(느린 접촉은 그마저도 없다).
  // 중립 행성·지구는 3 — 큐볼은 여러 번 굴려 쓸 수 있고, 지구도 한 번 스쳤다고
  // 끝나지 않는다. **조르그 요새만 1**이다: 제대로 한 번 처박으면 끝난다.
  // (표적을 세 번 맞히는 건 반복이지 난이도가 아니었다 — 플레이테스트.)
  PLANET_HP: 3, ZORG_HP: 1, EARTH_HP: 3,
  BOUNCE_RESTITUTION: 1.0,            // 행성끼리의 반발계수 — 1 = 완전탄성(운동량·에너지 보존)
  HIT_COOLDOWN: 0.7,                  // 같은 쌍의 재판정 금지 시간(인게임 초)
  COLLIDE_DMG_V: 6,                   // 이 상대속도 미만의 접촉은 데미지 없이 튕기기만
  // 가스 행성이 충돌로 유폭하는 문턱. 이웃 궤도끼리 스치는 자연 접촉(<10)은
  // 넘지 못하고, 핵으로 민 공(Δv = 520×작약/질량, 5Mt·μ200 이면 13)은 넘는다.
  VOLATILE_TRIGGER_V: 18,
}
export const radiusOf = (mu) => 1.6 * Math.cbrt(mu)   // T1-9
// 미사일 명중 반경 — 물리(행성끼리 충돌)는 여전히 b.radius를 쓴다.
// 이 함수는 "미사일이 무엇에 맞는가"에만 쓰이고, 렌더도 같은 값을 그린다.
export const hitRadiusOf = (b) =>
  b.type === 'debris' ? b.radius : Math.max(CFG.MIN_HIT_R, b.radius * CFG.HIT_R)
// 행성끼리는 "보이는 공"이 닿는 순간 터진다 — 판정과 그림이 같은 원이라
// "닿았는데 안 터졌다"가 원천적으로 없다.
export const contactDist = (a, b) => hitRadiusOf(a) + hitRadiusOf(b)
// 성계 반경 — 공을 열댓 개 깔려면 판이 그만큼 넓어야 한다. 다만 무한정
// 넓힐 수는 없다: 미사일이 TTL 안에 반대편까지 닿아야 판이 성립한다
// (LAUNCH_MAX × MISSILE_TTL ≈ 2900 GU 가 실질 사거리 상한).
export const aMaxOf = (A) => 1700 + 130 * Math.min(A, 6)   // §7.2
// 카이퍼 벨트 반경 — 성계의 쿠션. 최외곽 궤도(0.95 aMax)의 원점 거리보다
// 넉넉히 밖에 둬서 평시 공전이 벨트를 긁지 않게 한다.
export const beltRadius = (aMax) => aMax * CFG.BELT_MUL
// 미사일이 느끼는 탈출속도 — 미사일 중력장은 행성 μ를 κ배로 증폭해서 받는다
// (physics.fieldAccel). 그래서 "이 속도로 쏘면 저 천체를 벗어나는가"는
// 진짜 물리가 아니라 이 식으로 물어야 한다. 지구 발사 하한(LAUNCH_MIN)의 근거.
export const escapeSpeed = (mu, d) => Math.sqrt(2 * CFG.KAPPA * mu / Math.max(1, d))
// 핵 임펄스 — 작약량과 표적 질량만의 함수. 이 한 줄이 게임의 조준 규칙 전부다.
export const nukeDv = (yld, mu) => CFG.NUKE_IMPULSE * yld / mu
export const blastRadius = (yld) => CFG.BLAST_R * yld

// ─── 렌더 전용 상수 ───────────────────────────────────────────
// 물리 반지름 R_p = 1.6·μ^(1/3) 은 기획서 §4.1 고정값이라 건드리지 않는다.
// "행성이 커 보이게" 하는 건 전부 여기: 중력권 헤일로(§5.2 스윙바이 존 6R_p,
// SOI 확대율 κ^(2/5)≈5.1× 가 근거) + 화면 최소 크기 바닥값 + 3D 구체 셰이딩.
export const VIS = {
  FOV: 42,                    // 탑다운 원근 카메라 — 시점은 위에서, 입체감은 유지 (§14.1)
  // 최소 화면 지름(px) — **공이 아니라 표식(외곽 링)에만** 적용된다.
  // 공 자체는 언제나 판정 반경 그대로 그린다(Scene.renderRadius 주석 참고).
  MIN_PLANET_PX: 18,
  // 파편은 예외다 — 표식(외곽 링)을 아예 안 그리므로 이 바닥값이 공 자체
  // (Scene.syncBodies)에 직접 걸린다. 판정 반경 그대로 그리면 기본 줌에서
  // 4~5px 반경(=지름 7px 안팎)이라 배경 별빛·벨트 알갱이에 묻혀 사실상 안
  // 보였다 — 파편은 정밀한 당구공이 아니라 미사일 경로의 "장애물 표시"일
  // 뿐이므로, 판정보다 살짝 커 보이는 대가를 감수하고 최소 크기를 키운다.
  MIN_DEBRIS_PX: 16,
  // 공을 작게 만든 대신 부풀림 상한을 올렸다. 전체 뷰에서 20개가 점으로
  // 사라지면 "많다"가 정보가 아니라 노이즈가 된다.
  ICON_PX: 22,                // 행성 카테고리 아이콘 화면 크기(px)
  ZOOM_MIN: 0.42, ZOOM_MAX: 14, ZOOM_STEP: 1.4,
  FIT_MARGIN: 1.25,           // 자동 프레이밍 여백
  FULL_FIT: 1.58,             // §14.1 성계 전체 뷰 — 카이퍼 벨트(1.42 aMax)까지 들어가게
  // 스크린셰이크 — 세기는 화면 px로 잰다(월드 GU가 아니다. CameraRig.hit 참조).
  SHAKE_MAX: 46,              // 최대 세기 — 핵은 과장한다
  SHAKE_PX: 0.42,             // 세기 → 실제 화면 진폭(px) 환산. 46 ⇒ ±19px
  // 감쇠 진동수. 더 올리면 60fps에서 한 주기가 4프레임 밑으로 내려가 다시
  // 잔상으로 번진다 — 흔들림을 고친 이유가 그것이므로 여기가 상한이다.
  SHAKE_HZ: 10,
  SKY_FAR: 48000,             // 배경(은하 원반 z=-34000)까지 덮는 원거리면 여유
  PARTICLES: 16000,
}
