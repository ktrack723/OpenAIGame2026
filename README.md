# ORBITFALL

중력으로 계속 공전·자전하는 행성을 발사해 연쇄 충돌을 만드는 Three.js 기반 물리 로그라이크입니다.

## 실행

정적 파일만 사용하므로 `index.html`을 GitHub Pages로 배포하거나 로컬 정적 서버에서 바로 실행할 수 있습니다.

```bash
python3 -m http.server 8000
```

브라우저에서 `http://localhost:8000`을 여세요. 마우스와 모바일 터치 입력을 모두 지원합니다.
